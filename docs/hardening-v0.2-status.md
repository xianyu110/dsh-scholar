# 当前实现与目标规范差距

> 信息性文档，校准于 2026-08-11，最新审阅基线 `main@fda346b`。本文件描述当前仓库，不覆盖规范性文档。状态必须由源码、当前提交的本地验证，以及后续 CI 或结构化人工验收证据共同决定；历史测试计数、旧 README、零散截图和未绑定提交的日志不能继承为当前证据。

## 1. 状态定义与证据规则

状态只允许使用下列值：

| 状态 | 含义 |
|---|---|
| 未实现 | 没有可运行实现，或只有文档/占位代码 |
| 部分 | 已有骨架或局部能力，但存在规范缺口、已知阻断缺陷或关键验收缺失 |
| 已实现未验收 | 代码表面完整，但当前提交尚无全部阻断验收证据 |
| 已验收 | 当前 commit 的全部对应 acceptance 场景在目标环境通过；证据可来自零 SKIP CI，或结构化人工验收记录 |
| 已关闭 | 经明确决策不再实施；必须记录责任人、日期、边界和重新开启条件 |

“已实现”不等于“已验收”。开发期可先完成代码，将真实环境场景标记 `NOT_RUN_MANUAL_PENDING` 并继续后续实现；这类项最多进入“已实现未验收”。标记“已验收”时必须同时记录 commit SHA、acceptance 场景，以及 CI run/job 或符合 `manual-acceptance.md` 的人工记录。CI 中出现 SKIP、人工记录缺少环境/步骤/实际结果、缺少 Docker/TeX/DSH fixture、缺失 git base ref 或 `continue-on-error`，均不得计入 PASS。状态矩阵与“具体不一致”、USAGE、README 或源码冲突时，自动取较低状态并阻断完成声明。

## 2. 当前可复用基础

| 范围 | 当前可复用实现 |
|---|---|
| Kernel/CAS | TypeScript、node:sqlite、WAL、项目/Gate/Job/Artifact/Evidence/Claim/Budget、SHA-256 CAS |
| HTTP | v1 主接口、局部 v2 adapter、Zod 校验、binary Artifact、Terminal SSE |
| Plugin | Cordis Agent tools/commands/subagents、角色 ACL、四组 Skills、sidecar；无浏览器嵌入面 |
| UI | standalone 原生 DOM Workspace；Chat/Overview/Approvals/Runs/Terminal/Artifacts/Evidence/Manuscript/Budget |
| Runner | Docker/subprocess adapter、快照物化、lease、heartbeat、cancel、Terminal chunk、可选 Ed25519 Manifest |
| Analysis | Metric/RunSet/AnalysisPlan 与 paired bootstrap 基础 |
| Connectors | OpenAlex/Crossref/arXiv、缓存、Unicode 去重、部分失败透明 |
| TeX | 文件树、文本编辑、版本保存、快照、latex-compile、诊断/PDF 基础 |
| Release | 私有 bundle 清单、脚本化 verify/clean-room 基础 |

这些基础不代表符合 v2 规范；以下矩阵是后续执行的约束来源。

## 3. 校准后的目标差距矩阵

| ID | 目标 | 当前状态 | 当前阻断与关闭条件 |
|---|---|---|---|
| GOV-01 | 认证 Human Principal durable | 已实现未验收 | HTTP gate decision 路由 fail-closed(principal 必填,裸 actor→422);**本地 Principal resolver 已实现**:standalone BFF 从 bearer 凭据派生 DURABLE 会话身份(session.json,0600,确定性 sha256,重启稳定),所有转发请求带 x-principal-session,kernel decision 路由绑定该 session_id→决策重读完整保留 principal/tenant/auth_method/session;gate 决策经 BFF 正确解析 gate 所属项目做成员/角色检查(修复 gate id 被误当 project id 的 404 bug)。证据:tests/security/run-standalone-http-tests.sh 152/152(GOV-01 组:决策 200、session_id 持久化、session.json 0600、转发与文件一致)、run-gate-tests.sh 15/15;外部 IdP 可替换本 resolver,接入后即达已验收§4 P0 阻断项已关闭(commit f94fac5):BFF principal fail-closed(缺 principal 除 health/静态外 401)、客户端身份字段覆盖为会话身份、伪造身份负向测试(run-standalone-http-tests.sh 180/180 含 FC/forged 组)；**§5 P1（2026-08-11）已闭环**：PI-only capability route table（intake adopt、project archive/unarchive 加入 governance write）+ BFF/Kernel 双层校验 + membership 实时撤权，详见 §5 表 GOV-01/ONBOARD-01 行 |
| GOV-02 | Gate target freeze 与原子 Decision | 已实现未验收 | 五类 Gate（scope/idea/contract/budget/release）独立流程+映射；gate-state-cannot-transition（Gate 控制状态经 transitions 422）；concurrent-decision CAS 竞争 409 gate_already_decided 恰好一次；budget-gate-resume 仅接受 payload 声明的 resume_to；human-principal-durable 重读含 principal/tenant/auth_method/session；decideGate contract 分支校验跨项目 target（contract.project_id!==gate.project_id→422 contract_foreign）。证据：tests/security/run-gate-tests.sh 15/15（含并发决策、principal 重读、budget resume、跨项目 contract 422）、tests/unit/governance.test.ts 扩展；尚未绑定 CI job 报告，待验收§4 P0 阻断项已关闭(commit 22b2ec6):内部 approve 路由要求 x-service-token(浏览器 bearer/自报 principal 均拒→403 service_token_required)，contract approve 绕行 Human Gate 已封闭(负向:unit server 矩阵五路由×四种凭据、run-formal-binding-tests.sh 9/9)。 |
| API-01 | v2 + BFF AuthZ | 已实现未验收 | v2(handleV2):x-principal-id membership 404 + x-principal-role 角色能力(viewer/auditor 只读,researcher 禁治理写 transitions/gates/decisions/budget/approve/accept,非法 role→403 role_required);BFF 从自身 membership 解析角色并注入头(客户端值永不信任),BFF 层同策略纵深(403 role forbidden 稳定文案);CSRF 会话 token(/api 写强制,Origin 第二层)。证据:tests/security/run-standalone-http-tests.sh 180/180(角色矩阵、非成员 404、BFF 注入断言、FC/forged 组)、tests/unit/kernel.test.ts v2 角色用例;完整 /bff/research 面仍待验收§4 P0 阻断项已关闭(commit 22b2ec6):内部路由(jobs-claim/runner-keys/recover/leases/evidence verified+accept/contracts approve)独立 service bearer——kernel 配置 serviceToken 后强制 x-service-token(常数时间比较),浏览器 bearer Authorization 与自报 x-service-principal 均不满足(403 service_token_required);sidecar 自动生成 0600 dataDir/service-token 并以 env(DSH_SCHOLAR_SERVICE_TOKEN)传给 kernel/runner,BFF 注入自身凭据且不转发客户端头。负向:tests/unit/kernel.test.ts server 矩阵、run-evidence-tests.sh 13/13、run-hardening-tests.sh 18/18(claim bearer-vs-token)、run-formal-binding-tests.sh 9/9。 |
| EVID-01 | accepted Evidence only from 受控 Worker/Verifier | 已实现未验收 | 状态机 draft_unverified→verified→accepted 已实现：public 路由只创建 draft/legacy（伪造 provenance=verified/accepted→422）；POST /v1/projects/{id}/evidence/verified 要求 x-service-principal: analysis-worker 且 x-service-token（否则 403）；POST /v1/projects/{id}/evidence/{eid}/accept 要求 x-service-principal: verifier|auditor 且 x-service-token（否则 403），重验 RunManifest（verifyRunManifest）、Contract（approved）、RunSet（succeeded+metrics_artifact）、Analysis Artifact（artifact_refs 属本项目），记录 acceptance{accepted_by,accepted_at,request_id} 并 emit evidence.accepted outbox；跨项目 accept→422 evidence_foreign；verifyClaim 只接受 provenance=accepted（draft/legacy/verified 一律 inconclusive）。证据：tests/security/run-evidence-tests.sh 13/13（含无 token/自报 principal 403 负向）、run-lower-is-better-tests.sh 5/5、run-analysis-spec-tests.sh 29/29（manuscript 排除 draft evidence、verified→accept→manuscript 全链）、demo-full-flow 15/15（verified→accept→verify 全链）、单元测试新增 acceptEvidence 用例；尚未绑定 CI job 报告，待验收§4 P0 阻断项已关闭(commit 22b2ec6):verified/accept 内部路由要求独立 service bearer(x-service-token;自报 principal 单独不信任→403 service_token_required);draft/unverified Evidence 不进 Manuscript(claims_used=0 且语句缺失,spec 测试断言)。 |
| STAT-01 | 单一正式分析实现 | 已实现未验收 | resamples 固定 10,000(ANALYSIS_RESAMPLES,内核不再传 1000,artifact n_resamples=10000);minimum_n 由绑定 contract stop_conditions.min_completed_seeds 驱动,调用者降低→422 minimum_n_too_low(§12);canonical output key 顺序核对(worker 测试);golden vector 与字节一致性(STAT-01b)、MetricsFileV1、lower-is-better、Analysis→accepted Evidence 链均已实现;worker 公式按 §12 重写:seed=FNV-1a 32 over UTF-8 canonical JSON(plan+按 seed 升序的 run_ids+metric_values)、B=10000 生产固定、索引 CI floor(0.025(B−1))/ceil(0.975(B−1))、pLow/pHigh=(1+count)/(B+1) 两尾 raw_p、Holm 单调 step-down(名称平局)、effect_size=paired mean difference、数值 12 位有效数字 round-half-to-even、−0→0、重复 metric 名拒绝、零差→direction_ok=false 且 raw_p=1。证据:pnpm test 337/337(含 STAT-01 新用例)、run-analysis-consistency-tests.sh 25/25、run-analysis-spec-tests.sh 29/29(独立镜像重算 CI/p 相等);尚未绑定 CI job 报告,待验收§4 额外阻断已关闭(commit 22b2ec6):重复 seed→422 duplicate_seed(内核);统计公式与规范 golden vector 一致(公式重写+golden 重生成+spec 镜像断言)。 |
| STAT-01a | lower-is-better claim direction（acceptance-tests.md §6） | 已验收 | research-schemas：ExperimentContract.metrics 新增 `direction`（'higher_is_better'\|'lower_is_better'，默认 higher，experiment.ts:30-31）；EvidenceItem.result 新增 `direction`（evidence.ts:26-29）；kernel computeAnalysis 从绑定 contract 解析方向传给 analysis worker（kernel.ts:2160-2171，worker 本就支持）；verifyClaim 按方向解释 effect 符号：lower 时负 effect+CI<0→supported、正 effect→contradicted（kernel.ts:1983-2006）。证据：tests/security/run-lower-is-better-tests.sh 3/3，不再 SKIP，已默认启用并接入聚合器（run-all-v2-blocking-tests.sh SCRIPTS）；尚未绑定 CI job 报告，待验收验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| STAT-01b | 确定性 + golden vector + Analysis/图表/稿件数字一致（§6、§12） | 已实现未验收 | tests/fixtures/analysis-v1.json golden vector 已按 §12 公式重生成(resamples 10000、raw_p_value 字段、zero-diff case mz);analysis-worker.test.ts 断言相同输入双次运行输出逐字节一致且与 golden vector 一致;新增 tests/unit/analysis-determinism.test.ts(≥5 组输入矩阵,全部 10000 resamples);新增 tests/security/run-analysis-consistency-tests.sh 25/25:真实 docker 跑 baseline+formal 配对作业→analysis 两次调用 artifact 内容逐字节相同、analysis artifact 与 chart SVG 与 manuscript 的 mean/effect/CI 数字完全一致;新增 tests/security/run-analysis-spec-tests.sh 29/29:estimator 恒等(全精度 |mean(t−b)−(mean_t−mean_b)|<1e-9)、direction_ok 语义、独立 canonical-RNG-rule 镜像 CI、repeat 字节一致、manuscript 排除 draft evidence + accepted 链 claims_used=1;已接入聚合器(CI=1 下 17/17 PASS)。尚未绑定 CI job 报告，待验收§4 额外阻断已关闭(commit 22b2ec6):公式与 golden vector 一致(独立镜像重算相等,spec 29/29)。 |
| RUN-01 | 正式 Docker/快照/fencing/Manifest | 已实现未验收 | secure kind 拒 subprocess、approved Contract 绑定、严格 lease fencing、MetricsFileV1、data_artifact_ids 同项目+hash 校验、Run attempt(runs 表)、快照资源上限、**必签 Manifest 默认开启**(kernel requireSignedManifest 默认 true;runner 总是注册临时 Ed25519 密钥并签名;未签名 manifest→422 manifest_signature_required;单元测试显式 opt-out 只测无关路径)全部实现。证据:run-manifest-tests.sh 8/8(缺 artifact ref 用例已签名)、golden-v2/clean-room/release-bundle/demo 在默认签名下全绿;尚未绑定 CI job 报告,待验收§4 P0 阻断项已关闭(commit f94fac5):claim 返回 durable run_id、completeJob(succeeded) 必须带 run_manifest(422 run_manifest_required)、Job+Run 同事务、Analysis 重复 seed 422(run-fencing 15/15、terminal 11/11、analysis 25/25) |
| RUN-01a | secure 作业 Contract 绑定（P0，acceptance-tests.md §4 前段） | 已实现未验收 | kernel submitJob 对 baseline/pilot/formal/reproduce 强制：contract_id 缺失→422 contract_required；getContract 失败→422 contract_unknown；跨项目→422 contract_foreign；status!=='approved' 或缺 approval.gate_decision_id→422 contract_not_approved（kernel.ts:1303-1323）。server.ts 内部批准路由 POST /v1/projects/{id}/contracts/{cid}/approve（actor 必填，server.ts:386-394）现要求 x-service-token（service identity，浏览器 bearer/自报 principal 均拒→403 service_token_required），供 evals/orchestrator 用；交互路径走 Contract Gate 决策（GOV-02 原子冻结，approveContract 幂等，kernel.ts:992-1004）。证据：pnpm test 337/337（本会话运行，含 service-token server 矩阵）；tests/security/run-formal-binding-tests.sh 9/9、run-gate-tests.sh 15/15§4 P0 阻断项已关闭(commit 22b2ec6):approve 绕行封闭(service token 强制,浏览器不可达)。 |
| RUN-01b | 全字段 lease fencing（P0，acceptance-tests.md §4 末段） | 已实现未验收 | kernel heartbeatJob/completeJob 对 leased/running 作业：缺 generation/token→409 lease_stale；提供时精确匹配；无 owner-only 兼容放行（kernel.ts:1413-1432、1478-1485）。runner 侧所有 completeJob/heartbeat 调用点补齐 fencing 字段（workers/runner-gateway/src/index.ts:664-665、801、1028-1041；bin/runner.ts:133、157-158）。证据：tests/unit/kernel.test.ts §12.6 用例（519-595：stale token 409、缺字段 fail-closed）；tests/security/run-manifest-tests.sh 8/8（stale generation/token→409 lease_stale、当前 token→200，run-manifest-tests.sh:108-142）；专项 tests/security/run-fencing-tests.sh 12/12（heartbeat/terminal/complete 缺字段、旧/未来 generation、错 token 均 409 lease_stale；terminal frame 缺 lease_generation 已 fail-closed，内核不再默认放行）§4 P0 阻断项已关闭(commit f94fac5):Terminal frame 校验 owner/generation/token/attempt(409 lease_stale,负向测试) |
| RUN-01c | MetricsFileV1 强制（§4：formal/baseline/pilot/reproduce 成功必须存在 output_contract.metrics 指定路径的 MetricsFileV1） | 已实现未验收 | runner（workers/runner-gateway）对 secure 作业：缺 output_contract.metrics→code_error 失败；缺文件/非 MetricsFileV1（schema_version=1+非空 metrics）/run_id 不匹配/contract_id 不匹配/seed 缺失或非有限数/NaN/Infinity/重复 metric/非 Contract metric→不得 succeeded；secure 作业禁用 stdout fallback（index.ts:908-1011）。kernel submitJob 注入 payload.contract_metrics（contract primary+secondary，kernel.ts:1322、1330）；runner 注入容器环境 DSH_RUN_ID/DSH_CONTRACT_ID/DSH_SEED（index.ts:741-743）。证据（本会话运行）：golden-v2 29/29、clean-room 9/9、release-bundle eval 21/21 + release-bundle-tests 8/8、demo-full 15/15、demo-standalone 15/15，所有 secure 作业真实写 MetricsFileV1 文件；尚未绑定 CI job 报告，待验收§4 P0 阻断项已关闭(commit f94fac5):Metrics 文件路径 realpath 校验必须位于 outputs 内(../ 逃逸拒绝) |
| RUN-02 | 固定容器安全基线（images.lock digest 强制） | 部分 | Docker flags 基本具备；configs/runner-profiles/images.lock.json 已提交（schema_version=1，node_fixture=node@sha256:c610fcdf…，texlive=texlive/texlive@sha256:8957c916…）。kernel submitJob 对 secure 作业强制：baseline/pilot/formal/reproduce 缺 digest→422 image_digest_required；tag（node:22-alpine）、latest、锁外 digest→422 image_digest_untrusted；latex-compile 缺 digest 由内核注入锁内 texlive 条目，显式提供时也必须与锁完全一致（packages/research-kernel/src/images-lock.ts validateImageDigest；DSH_IMAGES_LOCK 可覆盖 lock 路径）。单元测试新增 4 个 digest P0 用例，pnpm test 228/228。所有 eval/security 脚本已改用锁内 digest。**smoke 宿主 subprocess 执行面已封闭（commit 040e796）**：runner 在 mode!=='docker' 时对未标记 `payload.trusted_fixture=true` 的 smoke job 一律 completeJob failed/failure_class=environment，错误消息含 trusted-smoke-fixture（execution-runtime.md §1）且带 lease_generation/lease_token fencing 字段；显式 trusted_fixture 的 smoke 仍可在隔离 subprocess 运行（echo 不变，docker 模式 smoke 不受影响）。源码修复：workers/runner-gateway/src/index.ts executeJob 前置拒绝（§3.2 门禁扩展 smoke 分支）；负向验收：subprocess runner + smoke（script 写宿主 marker）+ 无标记 → failed/environment 且宿主 marker 文件不存在（证明宿主未执行）；正向验收：同 runner + trusted_fixture=true → succeeded 且 metrics artifact 可断言。证据（本会话运行）：tests/security/run-hardening-tests.sh 21/21（含新增 smoke 负向/正向与适配的取消、空命令用例）、run-release-bundle-tests.sh 16/16（docker 模式 smoke 正常）、run-fencing-tests.sh 15/15、pnpm vitest tests/unit/kernel.test.ts 97/97；commit 040e796，§4 P0 行 RUN-02 已标注关闭。尚未绑定 CI job 报告，待验收§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。 |
| TERM-01 | 实时有序可恢复 Terminal | 部分 | reconnect-after-seq（按 seq 续传无重复无缺失）、retention-gap（淘汰 seq 返回 gap+dropped bytes）、overflow truncated+log Artifact 可下载、exit-replay（exit_code/signal/timed_out/cancelled 可读）、log-authz（跨项目 404）、cancel-timeout-distinct。证据：tests/security/run-terminal-tests.sh 11/11、tests/unit/terminal.test.ts；UI 侧 DOM 有界/ANSI 消毒/backpressure 属浏览器层，未纳入本行验收§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。 |
| PTY-01 | 可操作 Interactive Terminal | 部分 | 当前 Runner `stdio=['ignore','pipe','pipe']`，UI Terminal 只有 SSE 读取，无 stdin/resize/signal/PTY/reconnect control。关闭条件：独立 PtySession/Events、Local/Remote PTY adapter、WebSocket 或等价双向协议、AuthZ/generation/client_seq/TTL/gap、真实 TUI 浏览器验收；证明不能产 Evidence。**深 Interface/Schema/migrations 已完成（commit aeb71c9）**：research-schemas `pty.ts`（PtySession/PtyControlFrame client_seq 幂等/PtyOutputFrame server_seq+gap+retention/PtyOpenRequest preset+relative cwd）；kernel `pty-session.ts` 状态机 open→attached→detached→closed（attach/detach generation 递增、权限撤销→detach、idle TTL/lease/close→closed）+ PtyAdapter 接口/NullPtyAdapter 挂载点 + client_seq 幂等（重复回放、乱序/跳号 409）+ 输出 server_seq 单调 + retention_bytes 有界淘汰先发 gap；migration 0011（pty_sessions/pty_frames，SCHEMA_VERSION 10，幂等）；HTTP /v1/pty/*（POST sessions 校验后 adapter 未注册 501 pty_adapter_not_implemented 不创建惰性会话；control/frames 在 kernel-created 会话上完整可用，delivered=false 如实标注）；PTY 输出永不进入 Metrics/Manifest/Evidence/Gate（注释 + pty-not-evidence 断言）。证据：tests/unit/pty-session.test.ts 13/13（状态机/TTL/撤销/幂等/seq/gap/HTTP/not-evidence）、migrations.test.ts 8/8、pnpm test 全绿、research-schemas/research-kernel build 全绿。**LocalPtyAdapter 已实现（commit f54bfe3）**：`pty-local.ts` 真实 pseudo-terminal——Node 无内置 PTY，spawn `python3` 桥（嵌入脚本落 runtime dir），桥内 `pty.fork()` + `os.execvpe(preset shell)` 分配真实 tty；preset→argv 白名单（sh/bash/zsh/fish，宿主缺 shell→spawn 失败）、cwd 相对 workspace 根且 adapter 层防御性重校验（`..`/绝对路径拒绝）、env 缩减为白名单（PATH/TERM/LANG…，`$HOME` 重定向进 `<dataDir>/pty-workspaces/<workspace_id>` sandbox，host `$HOME` 不可达；桥再按 `DSH_*`/token/secret/credential 名称过滤）；信号按 pty 前台进程组投递（INT/TERM 中断/终止前台作业而交互式 shell 存活——真实终端语义；KILL 终止 shell 并产生 signal=SIGKILL 的 exit frame）；**断开不杀进程**（桥归 kernel adapter 所有，detach 后输出继续流入 store，reconnect 按 generation+after_seq 重放）；显式 close/idle TTL（kernel 持有 30s sweep 定时器）经 adapter.kill 拆除真实 tty，无孤儿进程组；输出经 server seq 帧化进 pty_frames（复用接口层 seq/gap/retention），exit frame 严格晚于最终输出；HTTP open 路由替换 501——注册真实 adapter 后要求 `x-principal-id`（BFF 注入）+ 项目 membership（422 principal_required / 404 project_not_found / 201 真实会话），control 携带他人 principal→403 pty_principal_mismatch；无 adapter 时保持 501 不建惰性会话；kernel bin 默认注册 LocalPtyAdapter（workspaceRoot=数据目录下 pty-workspaces），standalone/plugin sidecar 经同一 bin 获得真实 PTY。证据：tests/unit/pty-local.test.ts 11/11（真实 tty 回显/写回、resize、INT/TERM/KILL 前台进程组语义、detach 存活+reconnect 无重复、显式 close 拆 tty、idle TTL 拆 tty、env 白名单含 DSH token/host HOME 负向、cwd/preset 边界、HTTP open principal/membership、not-evidence）、pty-session.test.ts 13/13 不破坏、research-kernel build 全绿、node scripts/verify-docs.mjs 18/18。**剩余（如实记录）**：Remote 传输与浏览器 TUI（LocalDockerPty/RemoteRunnerPty 共享同一 wire、WebSocket 双向 attach、真实终端渲染浏览器验收）；idle TTL/retention 的 Config Schema registry 键。 |
| WORK-01 | VS Code 式通用 Workspace | 部分 | 当前只有 TeX text workspace 与有限 manuscript textarea；无通用 code/scratch tree、binary CAS、tabs/search/watch/upload、move/history UI、Problems 和集成 PTY。关闭条件：统一 Workspace Interface/DB/CAS/Revision/ETag，TeX 作为 facade，桌面/窄屏与冲突/路径/二进制验收。**深 Interface/Schema/migrations 已完成（commit aeb71c9）**：research-schemas `workspace.ts`（WorkspaceNode path/kind/media/size/revision/etag/hash、WorkspaceRevision、WorkspaceOp create/write/delete/move target version CAS、write/move/delete 请求 schema、workspaceEtag）；kernel `workspace-store.ts` 统一 code/scratch/manuscript 树（list/read/write/delete/move + workspace revision/每路径 version/strong etag、预期 version/etag 冲突 409、create-if-absent、二进制走 artifact CAS blob_sha256 服务端 sha256、文本写二进制 422 read-only、路径安全复用 snapshot-walk 契约、move 目标冲突 409、op ledger history、dir 前缀投影）；`tex-facade.ts` TexWorkspaceFacade 把 TeX store 映射到同一 WorkspaceStoreLike 契约（TeX 作为 facade，无第二套 bytes/revision 权威，顺带修复 tex history 死代码对不存在 revision 列的查询）；migration 0011（workspaces/workspace_nodes/workspace_ops，SCHEMA_VERSION 10，幂等）。证据：tests/unit/workspace-store.test.ts 8/8（revision/etag/409/二进制 CAS/路径安全/facade）、migrations.test.ts 8/8、pnpm test 全绿、research-schemas/research-kernel build 全绿。**磁盘 adapter 已实现（commit e298cee）**：`workspace-store.ts` 真实磁盘存储——每项目树根 `dataDir/workspaces/{project_id}/{workspace_id}/`（0750 链/0640 文件，ensure 即建根），节点字节以规范化路径落盘、`workspace_nodes` 只存元数据（磁盘 adapter 不写 content 列）；写=目标目录临时文件+原子 rename（无半写/无 tmp 残留）；revision 单调、etag=version+sha256[0..12] 绑定；任意字节二进制节点（multipart assets ≤32 MiB 复用 UPLOAD-01 上限，服务端 sha256，blob_sha256 同时注册 artifact CAS 幂等引用、工作字节以树文件为准）；大小上限 WORKSPACE_MAX_FILE_BYTES=32 MiB → 413 workspace_file_too_large；move/delete 带 target-version CAS（409）、move 原子 rename 磁盘文件；路径安全：规范化负向 + 磁盘层任意 symlink 拒绝（422 workspace_symlink，读/写/删/移动前 lstat，snapshot-walk 严格超集）；history：每路径保留最近 8 个版本字节于树外 `.ws-meta/{wsid}/history/{path}@{version}`，`readVersion(path,N)` 回退读（删除后仍可读，超窗 404）；watch/search：`listSince(revision)` 变更节点+deleted tombstone、`search` 路径前缀/glob（`*` 不跨 `/`），内容搜索未实现（如实记录）；server `/v1/projects/{id}/workspaces*` 全路由（create/list/tree/nodes 读读写删+watch+rollback/moves/search/assets multipart/blobs 原始字节），跨项目 404、BFF multipart 原字节透传 + membership/role 保持；TeX facade 对齐新契约（listSince 保守整树、readVersion 仅当前版本——tex store 无 per-op ledger/per-file 历史，取舍记录于 execution-runtime.md §12.2）。证据：tests/unit/workspace-store.test.ts 15/15（原子写/0750 根/无 tmp、revision-etag、409、move/delete CAS、二进制 round-trip、路径负向+symlink、历史回退+保留窗口、listSince/search、大小上限、per-project 根、facade）、tests/security/run-workspace-tests.sh 38/38（真实 curl 创建/写/读/移动/冲突/上传/下载/watch/search/history/跨项目 + BFF 透传与 401/403/404 负向，已接入 run-all-v2-blocking-tests.sh）、pnpm vitest 全绿、research-schemas/research-kernel build 全绿、node scripts/verify-docs.mjs 18/18。**剩余（UI 轮，如实记录）**：浏览器编辑器 UI（tabs/search/watch/upload、move/history 面板、Problems、集成 PTY）与桌面/窄屏/冲突/路径/二进制浏览器验收（Playwright 类环境不可用）；远程文件系统 adapter 未实现。 |
| TEX-03 | 保存后实时 LaTeX Preview | 部分 | 当前 save/build 分离且 2s 轮询，无 debounce/supersede preview、live build stream、freshness；保存冲突后仍可能继续 compile。关闭条件：save-success event→preview、旧 PDF stale、live log/diagnostics/PDF、显式 Compile 固定 manifest 且与 preview 权威分离。**kernel/逻辑层已闭环（commit 1907897）**：POST /v1/documents/{id}/preview-builds（server 端 debounce 默认 800ms 可配置、持久化 tex_preview_pending、Kernel 持有定时器，重启自动重挂）+ preview build 状态机（queued→running→succeeded/failed；queued→cancelled、running→superseded，superseded_by/at，job 尽力取消）+ 同一固定 texlive image/禁网/no-shell-escape 复用 latex-compile runner（payload.preview=true，不产 Evidence、不冻结权威 manifest）+ stale 判定（build.revision<document.revision→build 记录 stale=true）+ 权威 Compile 创建即 supersede 全部非终态 preview（活跃权威编译期间 preview flush 跳过）+ claim 时 build queued→running、cancelJob 同步 build、superseded/cancelled build 不被完成事件改写 + migration 0010（SCHEMA_VERSION 9，幂等）。证据：tests/unit/tex-preview.test.ts 12/12（debounce 合并、新 revision 取消 queued、running→superseded、stale、权威 supersede、preview 不产 Evidence、去重、重启持久化、previewAutoTrigger、HTTP 路由）、tex-kernel.test.ts 9/9、tex-build.test.ts 8/8、kernel.test.ts 100/100、migrations.test.ts 8/8、pnpm vitest 全绿、latex-compile-e2e 13/13 不破坏、node scripts/verify-docs.mjs 18/18。**剩余（浏览器层，Playwright 类环境不可用，如实保留）**：debounce 后自动出 PDF 展示、stale 标识 UI、live log/diagnostics/PDF 同页展示、preview 面板与权威 build 面板分离的浏览器验收。 |
| RUN-REMOTE-01 | Local Docker + 受控远端 Runner Fleet | 部分 | 当前 runner 只有 subprocess/docker，CLI 默认 subprocess；无 target/profile registry、mTLS Remote Agent、capability/health/draining、CAS resume/partition spool/Remote PTY。关闭条件：同一 ExecutionPlan/lease/run_id/Manifest 跨 Local/Remote，离线 fail closed 且不静默降级。**profile 注册表与 Job 固定 pin 已闭环（见 §8 PROF-01 行）**：opaque RunnerProfile 注册表（research-schemas runner-profile.ts）+ submitJob 注入 profile_id/config_hash + runner 复算校验（不一致 environment 失败）+ ExecutionPlan profile_config_hash pin。**接口/schema 层已推进（commit aeb71c9）**：ExecutionTarget port（prepare(plan)/start(plan)/attach(run)/cancel(run)/wait(run)）+ LocalDockerAdapter（workers/runner-gateway/src/execution-target.ts）——docker 执行路径收敛到 port（prepare zod 校验+深度冻结+fingerprint、start 复算断言，plan 不可变，变异→ExecutionPlanMutationError；buildLocalDockerArgs 纯参数映射与既有容器基线逐项一致；command/run_id 一律取自 plan，runDocker 引擎行为不变）；ExecutionPlan schema（research-schemas execution-target.ts：project/job/run/lease/profile/config/image digest/snapshot/artifact refs/limits/network/output contract + Ed25519 签名与 payload_sha256 复算，`.strict()` 拒绝 address/certificate/SSH bootstrap 等连接信息字段）+ RemoteAgentRegistration（opaque target_id/agent_id/capabilities{os,arch,runner_ver,images}/labels/health{status,last_seen}/cert_fingerprint）+ scheduledTarget 纯函数（capability 匹配、offline/draining 拒绝、无匹配→明确 retryable 错误；bound target 不可用默认不回退 LocalDocker、更不回退 subprocess，policy.allow_bound_fallback_to_local 显式开启才回退；policy 可配置 prefer local/local_only/allow_remote）+ InMemoryAgentRegistry（注册/心跳更新 health/offline 判定）+ RemoteRunnerAgent fail-closed stub（真实 mTLS 未实现，任何执行/注册调用抛 RemoteRunnerAgentNotImplementedError，绝不静默降级）。subprocess 明确为非 target 兼容层（trusted-smoke-fixture 专用，execution-runtime.md §1）。证据：tests/unit/execution-target.test.ts 12/12（plan schema/映射/签名 round-trip+篡改负向、不可变断言、adapter 契约、参数映射）、tests/unit/remote-fleet.test.ts 23/23（注册/心跳/offline、capability、调度纯函数、策略不静默降级、Config/SecretRef 边界、stub fail-closed）、pnpm vitest 新增两文件 35/35、research-schemas/runner-gateway build 全绿、run-terminal/run-fencing 等 docker 路径不破坏（详见 §4 行 96）。**wire 协议 + 服务端/代理端已实现（commit 009531c，mock 传输可测）**：协议定义 docs/remote-runner-wire.md（HTTP+JSON，全消息 zod schema 两端共享，research-schemas remote-runner-wire.ts）；服务端 RemoteFleetServer（remote-fleet-server.ts）——注册/心跳（acknowledged/accepted、未注册 404）、claim（kernel claimJobs 同路径 + 固定并签名 ExecutionPlan + target/capability 匹配分发；无匹配 target 留在 pending retryable 不静默改派；agent 断连 outstanding 保留、恢复 resume 同一 claim/run_id/lease）、frames（kernel terminal 语义 + owner/token 精确匹配，与本地 runner 同一 lease 头路径）、artifacts（staged+finalize，服务端复算 sha256，篡改 409 cas_hash_mismatch 不落库）、complete（转发 kernel completeJob，lease 过期旧 agent 409 lease_stale、claim 置 settled、后续写入全拒）、CAS（响应带服务端复算 sha256，代理端复算比对）；HTTP 面 attachRemoteFleetRoutes 用 x-service-token 等价实现（生产必须 mTLS，文档注明差异）；代理端 RemoteRunnerAgentImpl（remote-agent.ts）——注册→心跳→claims 轮询→plan 验签（fail closed）→CAS 输入拉取+复算 hash（不一致拒绝执行）→隔离 subprocess sandbox 执行（executor 可注入，生产可换 docker 路径）→按 generation/token 上报 frames/stage artifacts/complete（Ed25519 签名 manifest + fencing）；断网本地有界 spool（AgentOutboundSpool：frames 可淘汰并先补发 gap frame 不静默丢弃，exit_frame/artifact/complete 不可淘汰、满则本地失败 fail closed），恢复按序重放，complete 恒为该 run 最后一条 wire 消息，resume 幂等不重复执行；mock 传输 InMemoryFleetTransport（JSON round-trip）+ FailingFleetTransport（断网/按方法故障注入）+ HttpRemoteFleetTransport（真实 HTTP loopback）。证据：tests/unit/remote-wire.test.ts 15/15（注册/心跳、claim 匹配 capability、CAS hash 复算不一致拒绝、frames 单调/gap、artifacts staged+finalize+篡改拒绝、complete 签名+fencing、离线→spool→恢复重放、spool 有界淘汰+gap、lease 过期 complete 被拒、agent_offline、HTTP service-token 403 负向与全链路）、根 pnpm test 全绿、research-schemas/runner-gateway build 全绿、verify-docs 18/18（详见 §4 行 96 与 docs/acceptance-tests.md §4.2）。**剩余（如实记录，属后续阶段）**：真实 mTLS 证书链（CA 签发/吊销/轮换）与真实远端 sandbox 验收、跨主机网络分区故障注入、Remote PTY、浏览器 UI。 |
| CONFIG-01 | 所有配置由 canonical Config Schema 管理 | 部分 | 当前配置散落 Cordis schema、CLI、env 和 UI preferences，仅有极小 ExecutionConfig；无 scope layer、effective provenance、revision/hash、SecretRef、生成 Settings。关闭条件：registry 生成 Zod/JSON Schema/template/CLI/HTTP/UI，覆盖所有运行项和 security floor，运行中对象 pin hash。**服务端全量覆盖已落地（commit f64690b + 009531c）**：canonical Config Registry（packages/research-schemas/src/config-registry.ts，见 docs/config-registry.md）——7 个 ConfigScope（global/project/job/runner-profile/orchestrator/kernel/standalone）、45 个 dotted key 注册表（每键 Zod schema+default+secret/security-floor 标记+来源+env 别名；覆盖 ExecutionConfig/IntegrityConfig 全字段、kernel CLI、runner CLI、orchestrator CLI、standalone CLI、images.lock 路径与 digest、network_policy、DSH_* env 系列）；validateConfig 合并默认+拒绝未知键+值校验+security floor 违规拒绝（privileged/docker-socket/host-network/network_policy=none 非 none 网络/subprocess 模式非 none 容器网络/自动发布/--no-token 非 loopback/锁外 digest）+ effective config sha256 pin；parseCli(argv, scope) 注册表 CLI 解析——kernel/runner/orchestrator/standalone 四个二进制全部真实接入（未知 flag 拒绝、数字/布尔转换、secret 不回显；--help 打印注册表生成帮助）；生成物：JSON Schema（configs/generated/config.schema.json）、默认值 template（configs/generated/template.yml）、CLI help（configs/generated/cli-help.txt），全部由注册表生成并与 Zod 一致；运行中对象 pin hash：ResearchKernel.configPinHash+configRedacted（构造期校验 fail fast）、HTTP x-config-pin 头 + /v1/v2 health config_pin、GET /v1/config/effective（redacted 明文视图+pin）、GET /v1/config/schema（注册表 JSON Schema）、kernel CLI 写入 0600 endpoint 文件 configPin、runner CLI 启动打印 pin、standalone BFF 启动校验 + 响应头；createProject 项目级 execution+integrity 经 registry 校验（自动发布违规落库前拒绝）。证据：tests/unit/config-registry.test.ts 30/30（默认合并、未知键拒绝、parseCli 全 scope flag 映射/未知 flag 拒绝/数字校验/secret 不回显、secret 脱敏不进明文、floor 违规拒绝、生成物与 Zod 一致、pin 稳定/变更敏感）、tests/unit/kernel.test.ts CONFIG-01 组 5/5（构造 pin 稳定/敏感、health+响应头、config/effective redacted、config/schema、createProject floor 拒绝）、pnpm test 全绿、research-schemas/research-kernel/runner-gateway/research-orchestrator/research-ui build 全绿、run-hardening/run-fencing 不破坏、node scripts/verify-docs.mjs 18/18。**剩余（如实记录，属后续）**：Settings UI（浏览器层）由 /v1/config/schema + /v1/config/effective 生成（api-contracts.md §19 的 /bff/research/config/* 随 Settings UI 落地）、job scope 键（每 Job 策略仍由 runner-profile + Job payload 派生）、SecretRef 存储层（当前 secret 仍 CLI/env/0600 文件提供，仅 registry 层脱敏+pin）。 |
| ONBOARD-01 | Init/Resume/Upload + Grill Me 接入任意阶段 | 已实现未验收（kernel/服务端层 commit f54bfe3；client 向导接线 2026-08-11；浏览器视觉验收剩余） | 现有 create/new 总是 DRAFT + Scope Gate；无 Intake、scan、question taxonomy、phase proposal、Human adoption/merge 或安全阶段映射。关闭条件：research-onboarding.md 全契约与 pre-accept 零权威写、Gate/Run/Evidence 防伪、恢复/GC/幂等验收。**kernel/服务端层已实现（commit f54bfe3）**：research-schemas `intake.ts`（IntakeSession/IntakeArtifact/IntakeObservation(trust 钉死 observed_unverified)/GrillQuestion+AnswerView(human_assertion)/PhaseProposal/ImportMapping/AdoptionReceipt/IntakeProjection/HumanPrincipal/IntakeStatus，research-onboarding.md §3 状态机 draft→uploading→scanning→needs_input↔grilling→proposal_ready→awaiting_human→accepting→accepted + rejected/expired/failed）；kernel `intake.ts`（INTAKE_DDL 四表隔离、GRILL_TAXONOMY_VERSION=1 确定性 taxonomy（11 稳定 code 按 target_phase 裁剪）、静态安全扫描（扩展名白名单/可执行 deny→rejected、HTML/SVG/无扩展名→quarantined、magic 校验、ELF/PE→rejected、静态 secret 模式→quarantined 且值永不回显、av_available=false 如实记录、archive 解包限制留给采用时 code-snapshot walk）、SAFE_PHASE_LANDING §6 表（observed_phase 是 metadata；safe_project_status 只经 Kernel 状态机——新项目恒 DRAFT；required_gates 采用时只创建 pending，绝不伪造 IDEA_APPROVED/CONTRACT_APPROVED/RELEASED）、buildPhaseProposal 确定性提案（plan/risks/pre_accept_checklist/confidence/gaps/mappings/next_actions））；kernel.ts 方法 beginIntake/stageIntakeArtifact/removeIntakeArtifact/scanIntake/getIntakeProjection/listIntakes/getIntakeQuestions/submitIntakeAnswers/proposeIntake/adoptIntake/rejectIntake/expireIntakes/cleanupIntakeStaged + assertIntakeInProject（跨项目 404）；migration 0012（SCHEMA_VERSION 11，幂等）；server.ts `/v1/projects/{id}/intake*`（begin/list/resume 投影、multipart stage ≤32MiB 复用 UPLOAD-01 上限、DELETE artifact、scan、questions、answers/adopt/reject 均 principal fail-closed 422、propose；跨项目 404 intake_not_found）；adopt 单事务（CAS 提升+Artifact 行+阶段 pending Gate+metrics→draft legacy_unverified Evidence+log→ImportedRunObservation（绝不 TerminalLog）+AdoptionReceipt 钉 proposal/target revision+outbox intake.accepted；同 Idempotency-Key+hash 同 Receipt、异 hash 409、proposal/target revision 409）；expireIntakes（7 天 TTL）+ cleanupIntakeStaged（24h）GC 隔离 staged、accepted blob 永不触碰；提案 next_actions 复用 GUIDE-01 NextAction schema。证据：tests/unit/intake.test.ts 32/32（quarantine→scan→grill→propose→adopt 全链、答案不齐 422 question_required、pre-accept 零权威写（gates/decisions/artifacts/jobs/runs/evidence/claims/terminal/workspaces/tex 计数 0 + Outbox 无业务写 + CAS 空）、幂等（begin/adopt key+hash）、恢复（重启后 answers/proposal 完整）、GC/reject 清理、principal 必填、静态扫描规则、HTTP 全路由+跨项目 404）、migrations.test.ts 8/8（0012 幂等）、pnpm vitest（排除并行子代理 in-flight 的 pty-local.test.ts 后 526/526）、research-schemas/research-kernel build 全绿、node scripts/verify-docs.mjs 18/18。**client 向导接线已闭环（2026-08-11）**：Start 屏三入口显式选择（不自动 projects[0]）+ modals/intake.ts 真实向导 begin→stage→scan→grill→propose→adopt（投影续接、稳定错误码文案、principal 载荷）+ kernel next-action intake 覆盖动作（详见 §5 P1 ONBOARD-01/UPLOAD-01/GUIDE-01 行）。**剩余（如实记录，属浏览器/后续轮次）**：拖拽/向导 UI 浏览器视觉验收、quarantine/scan 进度展示、分块 offset/hash 恢复上传（服务端整文件 staged，UI 如实不做分块，支持续传/重传）与研究包 archive 解包扫描、TeX workspace/CodeSnapshot 采用物化、merge 冲突 keep/current/import/rename 交互、v2/BFF accept 面与 Agent tool 面、浏览器验收（Playwright 类环境不可用）。**§5 P1（2026-08-11）已闭环**：intake ADOPT 与 project archive/unarchive 加入 PI-only capability route table（BFF/Kernel 双层校验，researcher/viewer/auditor 403），详见 §5 表 GOV-01/ONBOARD-01 行。 |
| UPLOAD-01 | 用户文件与研究包上传 | 部分（服务端/BFF 层已闭环，浏览器层剩余） | 现有 v1 JSON/base64 Artifact 和 text-only TeX；BFF 强制 JSON，无 multipart/staged browser intake、binary Workspace asset 或 resume/preflight/quarantine。关闭条件：<=32MiB multipart + intake staged upload、hash/scan/path/archive/security/rollback/recovery UI 验收。**服务端/BFF 层已闭环（commit 1907897）**：kernel 原生 `POST /v1/projects/{id}/uploads` multipart 端点（uploads.ts 无依赖 multipart parser + staged→finalize）——≤32 MiB 单文件强制（`ResearchKernel.UPLOAD_MAX_FILE_BYTES` 可覆写，请求体 32 MiB+1 MiB envelope 上限，流式中途 413 `payload_too_large` 不完整缓冲）；服务端计算 sha256（stage 计算 + finalize 复算核对，客户端不可声明 hash）；路径安全（绝对路径/`..`/NUL/Windows 盘符/分隔符 → 422 `invalid_file_name`）；staged 上传（会话 id 命名的 `stage_<id>.part/.json` 于 CAS root 下 staged-uploads/，finalize 原子 rename 进 CAS blob 槽 + artifact 行，失败回滚零残留，缺 stage → 409 `artifact_stage_expired`）；幂等（同 project+sha256+file_name 重传 → 200 `reused:true` 原 artifact 不重复写）；GC/恢复（`cleanupStagedUploads(maxAgeMs)`，默认 TTL 24h，grace-period 同 CAS 孤儿 GC，finalize 后 blob 不受 GC 触碰）；BFF 透传（standalone 对 multipart 以原始字节 + 原 boundary Content-Type 转发，bearer/Origin/CSRF/membership/role 检查与其它 /v1 写一致，33 MiB 在 BFF 层即 413）；协议负向（415 unsupported_media_type、400 invalid_multipart、422 missing_file/multiple_files/invalid_kind、404 project_not_found）。证据：tests/unit/upload.test.ts 20/20（≤32MiB 成功含恰好 32 MiB、>32MiB 413 流式与静态上限双路径、路径穿越、hash 绑定、staged→finalize 原子与篡改回滚、幂等重传、GC、parser 单元）、tests/security/run-upload-tests.sh 18/18（真实 curl -F 上传/回读、33 MiB 413、路径穿越、BFF 透传+幂等+401/403/404 负向，已接入聚合器 run-all-v2-blocking-tests.sh）、pnpm test 437/437、research-kernel/research-ui build 全绿。**剩余（浏览器层，Playwright 类环境不可用，如实保留）**：拖拽上传 UI、intake quarantine/scan 展示、上传进度/恢复/冲突 UI 验收；研究包 archive 的 scan/quarantine 属 ONBOARD-01 intake（archive 路径/symlink/重复规范化路径由既有 code-snapshot walk 强制，本端点只登记原字节不拆包）。 |
| GUIDE-01 | 全程结构化下一步引导 | 部分 | **Kernel 层已闭环（commit f64690b）**：NextAction schema（code/label/reason/required/route/capability/revision/state + blocking/refs/required_by，research-schemas `NextAction`）；`nextActionProjection` 纯函数按状态机每阶段投影结构化动作（17 个 status 每个 ≥1 动作：scope_gate_submit/survey_run/idea_generate/idea_gate_approve/contract_register/baseline_reproduce/pilot_formal_submit/evidence_verify/manuscript_write/reviewer_run/release_bundle/release_gate/gate_resolve/budget_resolve/job_retry/project_stop/终态 done 等），含 BLOCKED_GATE+budget 超限→`budget_resolve` blocked、pending gate→`gate_decide`（base 已引用不重复）、失败作业→`job_retry`（attempts 耗尽→blocked+`repair_decision`）、required 缺口（`approved_contract`/`succeeded_runs`/`proposed_idea`）；`projectProjection` 返回权威 `next_actions_v2` 且 legacy `next_actions: string[]` 由非 done 动作 label 稳定派生；未知/未来状态退化 `code='unknown'` 只读动作不抛错。Intake/Grill 覆盖动作已落地（2026-08-11）：`intake_resume`/`intake_scan`/`intake_answer`/`intake_propose`/`intake_adopt`（capability=pi，`NextActionContext.intakes` 可选输入、projectProjection 从 listIntakes 投影，终态零动作，详见 §5 P1 行）。证据：tests/unit/next-action.test.ts 16/16、pnpm test（排除并行子代理 in-flight 文件后 355/355）、run-gate-tests.sh 15/15、verify-docs 18/18。关闭条件剩余：每阶段/Intake/失败/Gate 的浏览器验收（Playwright 类环境不可用，如实保留）。 |
| TRAJ-01 | 移植 DSH Web Trajectory | 部分（standalone 投影与拓扑 API 层已实现，commit e298cee；浏览器拓扑渲染/进入 child 与 10k node 浏览器验收剩余） | Scholar UI 无 trajectory tab/projection/history/timeline；DSH 组件依赖 slot/SessionHistoryFace，不能直接复用。关闭条件：standalone safe projection、Research vs Session 泳道、分页/SSE/gap/redaction/virtualization/inspect 与 10k node 验收。**standalone 投影与拓扑 API 层已实现**：research-schemas `trajectory.ts`（TrajectoryEntry/Page/Lanes、TopologyNode/Children、ChildDetail(breadcrumb)、ChildHistory、FollowupReceipt、ChildLinkInput——对齐 trajectory-subagents.md §2/§3/§4 契约，token 四桶/cost/permissions/retention 字段标注"服务端已覆盖(投影基础)/DSH session 集成与 UI 后续"）；research-kernel `trajectory.ts`（TrajectoryStore：只读 outbox 投影 `projectTrajectory`（`(event_seq,event_id)` keyset 分页、单页上限 500、total/has_more、`lane` 过滤）、`projectTrajectoryLanes`（research/session 双泳道各自游标）、redaction（白名单 summary、token/secret/绝对路径 scrub、240 字符截断，写入+读取双次脱敏）、child_links/child_history/child_followups 存储、exact-parent+breadcrumb（cycle-safe/orphan fail-soft/深度 32）、只读 history、one-shot 只读 followup（记录 message 返回 message_id，state_unchanged 断言，不冒充已执行）；KernelEventKind 新增 `trajectory.child.started/updated/followup`（session 泳道）；migration 0013（child_links/child_history/child_followups + `idx_events_project_seq`，SCHEMA_VERSION 12）；server.ts `/v1/projects/{id}/trajectory|trajectory-lanes|topology(+/children)` 与 `/v1/topology/{child_id}(*|history|state|followup)`——全部 principal+membership fail-closed（缺 x-principal-id 422 principal_required、非成员/未知 child 404）；standalone BFF child 路由 project 解析（childProjectId 镜像 jobProjectId）+ x-principal-id 注入；research-client 方法（projectTrajectory/projectTrajectoryLanes/projectTopology/childDetail/childHistory/childFollowup/registerChildLink/updateChildState）。证据：tests/unit/trajectory.test.ts 12/12（分页跨 bucket 平局续传、脱敏断言、10k 事件 21 页不丢不重、拓扑 tree/breadcrumb/exact-parent/orphan/cycle、history/followup 只读、HTTP 权限矩阵）、migrations.test.ts 8/8、pnpm test 555/555、research-schemas/research-kernel/research-client build 全绿、research-ui typecheck+build 全绿、verify-docs 18/18。**剩余（如实记录）**：浏览器拓扑渲染/进入 child/breadcrumb 导航、SSE 实时流（after_seq replay/gap/revoke）、10k node 浏览器 DOM 虚拟化与键盘/ARIA/zh-en 验收（Playwright 类环境不可用）、research_panel 插件侧 registerChildLink/updateChildState 接线（插件代码本轮未动，kernel/服务端已就绪）。 |
| SUBAGENT-01 | 可见拓扑并进入 child | 部分（standalone exact-parent/breadcrumb/history/followup API 层已实现，commit e298cee；插件接线与浏览器拓扑渲染/进入 child 剩余） | `research_panel` 能启动独立 child 并返回 summary/child_id，但 UI 无 direct-child tree、实时 activity、breadcrumb/enter/history/followup；无 standalone exact-parent API/redaction。关闭条件：trajectory-subagents.md list/history/stream/followup、one-shot 只读、history 不激活、权限/脱敏/a11y 验收。**standalone 拓扑 API 层已实现（见 TRAJ-01 行）**：child_links 拓扑存储（child_id/project_id/parent_id/label/summary/mode/role/state/ended_at，re-register 不复活终态、state 只经 PATCH 变更、ended_at 首次终态钉定）、direct-child tree（`GET /v1/projects/{id}/topology?parent_id=`，只返回 exact direct children + has_children/children_count/seq 游标）、exact-parent+breadcrumb（`GET /v1/topology/{child_id}`，cycle-safe/orphan fail-soft）、只读 history（`GET .../history`，started/registered/state/followup 追加账本，读取不激活、不改变 state——断言）、one-shot 只读 followup（`POST .../followup` 返回 message_id、read_only=true、state_unchanged=true、不冒充已执行）、权限（trajectory/topology 全部路由 principal+membership fail-closed，BFF 先解析 child→project 再查成员，kernel 侧 x-principal-id 422/非成员 404）、脱敏（child summary 写入+读取双次 redaction+240 截断）。证据：tests/unit/trajectory.test.ts 12/12（含拓扑 tree/breadcrumb/orphan/cycle/history/followup 只读/HTTP 权限矩阵）、pnpm test 555/555（详见 TRAJ-01 行）。**剩余（如实记录）**：research_panel 插件侧调用 registerChildLink/updateChildState（本轮未动 plugin 代码）、浏览器 direct-child tree/进入 child/breadcrumb/activity 渲染、real-time stream、exact live-parent + capability 校验（需 DSH host）、a11y/键盘/zh-en 浏览器验收（Playwright 类环境不可用）。 |
| UI-SIMPLE-01 | 简洁页面与 Settings 渐进披露 | 已实现未验收（client 逻辑层 commit 1907897；Start 三入口显式选择 2026-08-11；浏览器层剩余） | **逻辑层闭环**：(1) Start 三卡——未选中项目即显示三张启动卡（新建研究/打开已有项目/上传·接入），定义于纯模型 nav.ts startActions()（code/route 稳定契约，label/desc 随 locale 求值），DOM 层按 route 接线：new-project→openNewProjectModal、open-project→openProjectSwitcherModal、import→**真实 intake 向导 openIntakeModal（2026-08-11，modals/intake.ts，不再占位 toast）**；Start 屏内嵌「打开已有项目」列表（filterProjects/pickProject 纯函数显式选择，`projects[0]` 自动回退已删除）。(2) 四 primary tabs + More——主导航收敛为四个 primary（总览 phase/运行 runs/证据 evidence/文稿 manuscript）+ More 下拉（对话 chat/Gate gates/产物 artifacts/预算 budget/终端 terminal + Settings modal），分组为纯函数 tabGroups()（nav.ts，全覆盖：ALL_TAB_KEYS 每键恰属一组）；More 每项稳定深链 `#tab=<key>`/`#settings`（parseDeepLink，query 路由不受影响；reload/back-forward 可达，hashchange 应用），Alt+1..9 沿 navOrder() 覆盖 primary→More→Settings；tab 恢复 TAB_IDS 对齐 ALL_TAB_KEYS（含 manuscript/terminal）。(3) Settings 渐进披露——内容分组为 Accordion（默认折叠、展开记忆可选，localStorage dsh-scholar-ui-settings-open），分组为纯数据 settingsSections()（settings-model.ts：连接/外观/偏好/runner/workspace/terminal/TeX/agent/config provenance 九组，每项带来源/生效状态 summary 行）；config provenance 有 registry 数据（/v1/health config_pin）时显示 pin hash，否则占位并如实记录；runner/workspace/terminal/TeX/agent 为诚实占位（后续阶段生成 UI）。证据：tests/unit/ui-simple.test.ts 15/15（三卡、三入口显式选择、四+More 全覆盖、九组默认折叠、深链 id 稳定、zh/en 键存在+parity+零缺 key、locale 重求值）、i18n-chrome.test.ts 3/3（R2 硬编码扫描覆盖新 chrome）、i18n-runtime.test.ts 12/12、research-ui typecheck+build 全绿、根 pnpm test 全绿（详见 §4 行 98 与 §5 P1 行）。**剩余（浏览器层，如实记录）**：640/720/1024 视口、键盘导航/a11y、Start/More/Accordion/导入向导的 DOM 有界断言需 Playwright 类环境，未验收。 |
| TEX-01 | TeX workspace/editor/version | 部分 | expected_version 语义修正:0=create-if-absent(UI 新建文件路径),HTTP schema 由 positive 改为 nonnegative;已存在+0→409 冲突;tree/GET 返回 path/kind/media/version(TexFileKind + fileMediaType 派生);保存 expected version→新 revision、并发 409、delete/move 版本校验;compile 冻结 manifest(既有)。证据:tests/unit/tex-workspace.test.ts + tex-build.test.ts 12/12、curl 集成(0→200、重复→409、tree 含字段);dirty 判断/窄屏布局属 UI 层待验收§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。**§4 行 95 kernel/工作区层闭环(commit 64a0649)**:snapshot 冻结可物化字节(tex_snapshot_files 表 + GET /v1/documents/{id}/snapshot-files?revision=&path=;tex-workspace.test.ts snapshot-frozen-bytes 用例、tex-build.test.ts HTTP 集成);dirty 基线修复(UI 用文件 GET/最近保存内容,不再查 tree——tree 无 content;清空非空文件('')dirty=true、revert 到已保存字节 dirty=false,manuscript-dirty.test.ts 4/4);clear/revert 与 revision CAS 覆盖(清空='' 是 CAS 可见变更、过期 expected_version 一律 409,tex-workspace.test.ts clear/revert 用例);保存冲突终止编译(见 TEX-02 行)。dirty 的浏览器展示(编辑即时 dirty 标识、窄屏布局)仍属 UI 浏览器层,Playwright 类环境不可用,未验收。 |
| TEX-02 | latex-compile/诊断/PDF | 部分 | 锁内 texlive digest 强制（缺 digest 内核注入 texlive@sha256:8957c916…）；compile 冻结 manifest；诊断含 file/line 定位（tex-diagnostics.ts）；shell-escape/network 拒绝（docker flags）；build history 可重放。证据：evals/latex-compile-e2e.sh 13/13（含 write18 inert）、tests/unit/tex-kernel.test.ts；实时 Build Terminal/freshness 展示属 UI 层§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。**§4 行 95 kernel/工作区层闭环(commit 64a0649)**:Runner 只物化冻结 revision 的可物化字节(getDocumentSnapshotFile→snapshot-files 接口,逐文件 hash 校验 manifest;不可读/hash 不匹配/路径逃逸一律硬失败,tex-build.test.ts 负向:revision 不可物化、字节篡改、traversal);compile 期间文件被修改,构建输入仍是冻结 revision 字节(tex-kernel.test.ts frozen-bytes 用例 + HTTP snapshot-files 集成);保存冲突终止编译:冻结与提交两处 document-revision CAS(texSnapshot expectedRevision + submitJob 重校验),carried manifest revision 与 tex_revision 不一致→409,409 后不创建 Job/build 行(tex-kernel.test.ts save-409 用例、tex-build.test.ts HTTP 集成);提交响应携带 build.job_id/build.revision/job 供 UI 接入 Terminal SSE 与 stale 判定(execution-runtime.md §12)。Manuscript 页内嵌 live Terminal DOM、PDF freshness 浏览器展示、防重复按钮浏览器验收仍属 UI 浏览器层,Playwright 类环境不可用,未验收。 |
| UI-01 | standalone-only 单一浏览器 UI | 已实现未验收 | 根嵌入面和旧 bridge 已删除；需当前提交的 clean package、真实 404、无 host/slot/dshClient 负向 CI 证据已绑定 ci-gate.sh 运行(commit 6e4d302)的类型检查/静态层面;浏览器级场景(键盘/a11y/响应式/DOM 有界)需 Playwright 类环境,待验收 |
| UI-02 | 全页面 zh/en i18n | 已实现未验收 | localeParityReport/assertLocaleParity(zh/en key 精确一致,dev warning + build/CI 硬失败);i18n-static-chrome 静态扫描测试(tests/unit/i18n-chrome.test.ts 3/3:扫描 client 源码硬编码中文与 chrome 位置英文字面量,命中即 fail,已修真实硬编码;zh/en key 集合一致断言);全部命名空间双语;根 pnpm test 已覆盖。证据:pnpm test 316/316;浏览器级 a11y/键盘/响应式仍待 Playwright 类 UI 验收已绑定 ci-gate.sh 运行(commit 6e4d302)的类型检查/静态层面;浏览器级场景(键盘/a11y/响应式/DOM 有界)需 Playwright 类环境,待验收。**§4 行 99 逻辑层闭环(commit f64690b)**:locale 切换运行时重建——i18n adapter 新增缺 key 告警(t() 全字典缺失时返回 raw key,dev console.warn 一次/键+字典路径提示,setMissingKeyReporter 注入收集器可测)与 open-overlay registry(setLocale→listener+relocalizeOpenOverlays,已开 modal 重开);shell 静态 chrome(header/tabs/model/density/aria/document title)由纯 chrome 模型 i18n/chrome.ts 渲染时求值并随 locale 重绘(不再初始化快照);pipeline 步骤由模块级 PHASE_PIPELINE 快照改为 phasePipeline() 渲染时求值;Terminal 元数据/exit 硬编码英文改 terminal.meta/exitLine 键(zh/en 含 dropped/truncated/exit 片段);状态 pill/sidebar/search 状态文案改 status 命名空间 statusLabel()(en 保持 kernel enum 镜像、zh 翻译、unknown enum 原样显示)。证据:tests/unit/i18n-runtime.test.ts 12/12(切换重建 tab/pipeline/overlay registry/Terminal/状态 pill、缺 key 告警、zh/en 往返、parity 保持)+ i18n-chrome.test.ts 3/3、research-ui typecheck + build(tsdown+verify-research-ui-bundle)全绿、根 pnpm test 全绿;浏览器级(键盘/a11y/窄屏、一个 render tick 内 DOM 断言)需 Playwright 类环境,如实保留 |
| UI-03 | standalone locale/theme adapter | 已实现未验收 | 解锁页(首屏+token gate)双语:BOOTSTRAP_HTML 内联 zh/en 字典 + data-i18n 键 + html lang 按 persisted dsh.locale→navigator.languages→zh;invalid-token 文案双语且键存在于 zh/en 字典(packaging.test 断言 data-i18n 键与字典一致性);locale/theme 持久化与 adapter 既有。证据:packaging.test.ts 新增解锁页双语用例(10/10)、首页 curl 含 data-i18n 键;首屏闪英文仍可能有残余,待 UI 验收已绑定 ci-gate.sh 运行(commit 6e4d302)的类型检查/静态层面;浏览器级场景(键盘/a11y/响应式/DOM 有界)需 Playwright 类环境,待验收。**§4 行 99 关联(commit f64690b)**:persisted dsh.locale→setLocale→全页重建的运行时闭环(i18n adapter overlay registry + chrome 模型重绘 + panels/Terminal/状态重渲染)已由 tests/unit/i18n-runtime.test.ts 逻辑层覆盖(见 UI-02 行);解锁页浏览器级切换仍待 Playwright 类环境 |
| UI-04 | browser client strict typecheck/模块边界 | 已实现未验收 | client/index.ts 由 ~8400 行拆分为 1070 行装配入口 + 模块(api/state/ui/types/sidebar/chat/terminal + panels/(phase/gates/runs/artifacts/evidence/budget/manuscript) + modals/(commands/detail/project/search/settings)),模块边界按职责;根 package.json "test" 现在包含 pnpm --filter @dsh-scholar/research-ui typecheck(tsc --noEmit strict,不再只靠 tsdown 转译)。证据:research-ui typecheck/build 全绿、pnpm test 304/304、standalone 新 client.js 正常服务;尚未绑定 CI job 报告,待验收已绑定 ci-gate.sh 运行(commit 6e4d302)的类型检查/静态层面;浏览器级场景(键盘/a11y/响应式/DOM 有界)需 Playwright 类环境,待验收 |
| ART-01 | binary/SSE 真流式 | 已验收 | binary round-trip(media type/ETag/bytes)已有;SSE 真流式验收:tests/security/run-sse-tests.sh 23/23——text/event-stream 真实 body、连接后 live tail、after_seq 续传无重复无缺失、跨项目 404(kernel 与 BFF 双面)、BFF 无 token/wrong token 401、非成员在首字节前 404;撤权(revoke-on-disconnect)与背压源码未实现,已注明未覆盖验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| ART-02 | media type/Range/hash | 已验收 | media type、ETag/Range 基础存在；需 project-scoped v2/BFF、hash 重验和当前 CI 证据验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| STORE-01 | 显式迁移与 schema parity | 已验收 | SCHEMA_VERSION 6→7,迁移 0008_outbox_envelope:events 表增 outbox 列(event_seq/event_version/aggregate_*/request_id/session_id/attempts/last_error/next_attempt_at/dead_lettered_at)+聚合内唯一索引、新增 runs 表(job attempt 行,§3.1 parity)、session_links 增 principal/tenant/issuer;幂等且兼容旧库。证据:tests/unit/migrations.test.ts 全绿(schema 升级后列存在、旧行回填 event_seq);尚未绑定 CI job 报告,待验收验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| STORE-02 | durable CodeSnapshot | 已验收 | 快照资源上限:SNAPSHOT_MAX_FILES=10000/MAX_FILE_BYTES=64MiB(stat 后先拒再读)/MAX_TOTAL_BYTES=512MiB,超限 422 snapshot_too_large;宿主绝对路径不再写入 archive/metadata(root 占位符);path/traversal/symlink 逃逸保护保留;data_artifact_ids 同项目+hash 可重验(RUN-01 补充:缺失 422 data_artifact_missing、跨项目 422 data_artifact_foreign)。证据:tests/unit/kernel.test.ts 新用例(上限、root 不含绝对路径、data refs 三态);尚未绑定 CI job 报告,待验收验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| EVENT-01 | 事务 Outbox | 已验收 | events 写入带 event_seq(max+1 单写事务,聚合内单调)+event_version=1+aggregate 身份+request/session 跟踪+attempts/dead-letter 字段;emit 在调用方事务内复用同一事务(node:sqlite 无嵌套 BEGIN 处理);已淘汰 seq 回填。证据:tests/unit/kernel.test.ts 连续 emit 断言 event_seq 单调递增、字段存在;尚未绑定 CI job 报告,待验收验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| DSH-01 | DSH Agent tools/commands/lifecycle | 部分 | canonical 工具名+deprecation 别名、/research 子命令真实 handler、skill 包根解析+确定性选择、npm pack assets、headless 可用(既有);新增:未知/未注册 Agent 默认 role=none,研究写工具全部 deny(ACL 测试断言);插件 dispose 清理(sidecar.stop/cache)测试。证据:tests/security/run-dsh-plugin-tests.sh 41/41(含 unknown-agent deny 与 disposer 用例);隔离 DSH host fixture 全链仍待 CI-01§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。**§4 行 100 P1 阻断项已关闭(commit 64a0649)**:apply 改为 async 且被 Cordis await——`sidecar.start()` 完成后才发布 `ctx.research`/client(endpoint 为 port=0 解析出的真实端口)与工具/命令/skills;所有 register/on/provide/plugin 资源由 Cordis effect 模型随插件 fiber 回收(工具/命令注册经 dsh-scope ScopedLayers→ctx.effect),sidecar disposer 在 apply 开头注册(启动期间 dispose/reload 也停 kernel);工具上下文由模块级 toolContextRef 改为注册时实例闭包,多实例互不串线。证据:run-dsh-plugin-tests.sh 47/47(新增最小 Cordis host fixture 组:真实 cordis Context + 真实 ToolRegistry/CommandService/SkillService,覆盖 port=0 真实 endpoint+client 可用、双实例隔离、cordis update 重载无重复注册、dispose 全回收+端口释放+同 root 重载可用),tests/unit/sidecar.test.ts 8/8;真实 DSH 宿主完整 boot 仍不可用,以最小 cordis host fixture 等效验收并如实记录(见 §4 行 100)。 |
| SIDE-01 | sidecar identity/ownership | 部分 | kernel 支持 --endpoint-file(port 0 时上报实际端口);sidecar(插件+standalone)复用前校验 0600 runtime/endpoint.json 的 protocol/schema/database/dataDir,缺失→sidecar_identity_unknown、不匹配→sidecar_identity_mismatch,均拒绝复用且绝不 kill 非本实例进程;spawn 后写自身 endpoint.json,stop 时按 pid 清理。证据:tests/unit/sidecar.test.ts 8/8(同 dataDir 复用、跨 dataDir 拒绝且原 kernel 存活、port=0 实际端口、无文件拒绝);运行中 standalone 的 endpoint.json 已生效(0600);尚未绑定 CI job 报告,待验收§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。**§4 行 100 P1 阻断项已关闭(commit 64a0649)**:插件 apply await `sidecar.start()` 完成后才读取 `sidecar.endpoint` 并发布 client/`ctx.research`——port=0 时 endpoint 恒为真实绑定端口;sidecar disposer 在 apply 开头注册,启动期间 dispose/reload 亦停 kernel(owned endpoint.json 按 pid 清理)。证据:run-dsh-plugin-tests.sh 47/47(host fixture port=0 组:endpoint 与 0600 endpoint.json 一致、client 真实 kernel 调用、reload/dispose 后端口释放+进程终止),tests/unit/sidecar.test.ts 8/8。 |
| SKILL-01 | Skills 安装发现与确定性选择 | 已验收 | provider 从发布包根解析四组 skill（research-core + 2 domain + venue），npm pack 断言 assets 在包内；domain/venue 按 Brief 确定性选择（skills.ts selectSkillPacks）；证据：run-dsh-plugin-tests.sh 36/36；clean install hash 一致性仍待 DSH fixture 验收验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| PACK-01 | clean remote install | 已验收 | 7 包 tarball 完整;clean consumer install 用 file: 覆盖 + overrides,断言 node_modules 是真实文件非符号链接、无原 checkout/../test-lzszq 路径、@deepseek-ai/* 宿主 peer optional 且无 registry 环境不强制安装;skills frontmatter 校验。证据:tests/unit/packaging.test.ts 10/10;真实 registry 发布仍待(环境无 registry,以 tarball+overrides 等价验收)验收绑定:commit 6e4d302 + scripts/ci-gate.sh 全量运行(4/4 PASS,142s,零 SKIP,日志 /tmp/ci-gate-full.log)+ 对应 acceptance 场景(阻断脚本 CI=true 零 SKIP)。 |
| SELFMOD-01 | dev-only Cordis self tools | 已实现未验收 | production 静态否定(tarball/lib/src 无 cordis self 工具字符串、依赖图无 dsh-tool-cordis、verify-docs fail-closed)+ **真实 DSH host fixture 动态验收**:隔离 DSH_HOME 下 --profile web --dump-config 生产 0 引用、叠加 dev overlay patch 后 tool-cordis 出现在组合配置树(2 引用)、无 opt-in env 时 start-selfmod-dev.sh 拒绝启动。证据:tests/security/run-selfmod-tests.sh 19/19(含 host fixture 组,已接入聚合器);cordis_inspect 六类信息的会话级 inspect 仍属 harness 深层交互,记录为后续 |
| REL-01 | 自包含 Release Bundle + bundle-only clean-room | 部分 | build-bundle.sh 在 manifest 增加 runtime 段（node 版本、kernel_bin/runner_bin sha256、images.lock 的 node_fixture/texlive digest）；reproduce.sh 把 bundle 复制到空目录执行、拒绝指向原 checkout 的 KERNEL_BIN/RUNNER_BIN（external checkout access prohibited）、node 版本不匹配→fail、作业重放强制使用锁内 digest；reproducibility-report.json 增加 bundle_manifest_sha256、runtime_verified、images_used、compared{manifest_hash,metrics,analysis,run_manifest,tex}，任一 false→status=fail。证据：tests/security/run-release-bundle-tests.sh 16/16（含 checkout 拒绝用例与 report 新字段断言）、run-release-eval.sh 21/21（reproduce 段 pass）；尚未绑定 CI job 报告，待验收§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。**§4 REL-01 P1 阻断项已关闭（commit 040e796）**：build-bundle.sh 把成功 latex-compile 作业引用的 TeX workspace 导出进 bundle（tex-workspace/<doc>/<path> + manifest.tex 每文件 sha256/大小）；reproduce.sh 在全新 kernel 中重建 TeX document（old→new 映射）、经 POST /v1/documents/{id}/builds 以原 idempotency key 重建 latex-compile 作业（HTTP jobs 路由 schema 不接受 latex-compile kind）；compared 升级为逐字段：metrics（name/unit/value 容差+seed）、analysis（mean 容差+n/effect_size/baseline_value）、run_manifest（key 集合+数量+每 key kind+run_manifest 存在性）、tex（TeX 输入文件清单+每文件 sha256、PDF 字节大小）；报告新增 cleanroom{snapshot_dir,kernel_db,kernel_cas,work_dir}（全新 mktemp 目录，不含原 checkout/bundle 路径）与 tex_comparison 明细；负向测试新增：原 checkout 改名后重放仍 pass（不读原路径）、checkout 外 digest 不匹配二进制立即拒绝（sha256 do not match）、node 版本不匹配→status=fail。证据：tests/security/run-release-bundle-tests.sh 25/25（含断言 8-12）、run-release-eval.sh 25/25（含真实 texlive latex-compile 段，reproduce 段 pass）；commit 040e796，§4 表第 101 行已标注关闭。尚未绑定 CI job 报告，待验收§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。 |
| CI-01 | 完整阻断 CI | 已实现未验收(本地网关) | 用户决策不用 GitHub Actions;新增 scripts/ci-gate.sh 本地 CI 网关(一条命令:pnpm test+verify-docs+CI=true 聚合器+plugin typecheck,exit 非零即阻断,--skip-security 可选并注明降级);根 package.json test:ci。证据:bash -n 通过、各步骤单独全绿;DSH host fixture(SELFMOD 动态 overlay、Agent 全链)仍待外部 host 环境 |
| DOC-01 | Markdown 是生成权威 | 部分 | 2.3 文档集含 research-onboarding 与 trajectory-subagents；verify-docs.mjs 对 18 篇文档的结构/链接/关键 contract/删除面/SELFMOD 违规 fail-closed，`node scripts/verify-docs.mjs` 当前通过 18/18。`--diff-check` 和持续语义一致仍需当前提交/CI 证据，不能把静态验证当功能验收。§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。 |
| DOC-02 | change-aware docs sync | 部分 | --diff-check 覆盖扩展到根 src/、configs/、migrations/、scripts/(除自身)、tests/(unit+security)、docs/、evals/;改动范围必须伴随 ledger 移动;base ref 不可达保持 fail-closed。证据:node scripts/verify-docs.mjs --diff-check origin/main 通过(本会话全量改动已同步);待长期语义一致性持续验证§4 审阅校准为较低状态:见第 4 节对应阻断项与强制关闭条件。 |

## 4. 2026-08-10 当前代码审阅阻断项

审阅范围：`7adc722...cf6a8a8`（`main` 与 `origin/main` 一致，109 个文件，约 `+18,252/-8,022`）。本节是对 §3 历史实现声明的当前校准；同一 ID 有冲突时，必须采用本节的较低状态，直至源码修复、负向验收和当前提交证据同时关闭。

| 优先级 | 影响 ID / 当前状态 | 当前阻断 | 强制关闭条件 |
|---|---|---|---|
| P0 | GOV-01、API-01：✅ 已关闭（commit f94fac5 + 22b2ec6） | standalone 默认 `principal=null`，membership 直接放行；v2 create 继续信任 body 的 creator/tenant，项目列表在 Principal 缺失时返回全量。 | 缺失/非法 Principal 对除 health 外的 list/create/read/write 全部 fail-closed；BFF 从认证会话构造 Principal 并覆盖/忽略客户端 creator/actor/tenant；补默认启动、伪造身份和跨项目负向测试。 |
| P0 | API-01、EVID-01：✅ 已关闭（commit 22b2ec6） | `jobs-claim`、`runner-keys`、`recover/leases` 无 service 身份认证；Evidence verified/accept 只比较可伪造的 `x-service-principal` 字符串。 | internal route 使用独立 service bearer/mTLS 或等价不可伪造身份；浏览器 bearer 和自报 header 均拒绝；密钥注册、claim、recovery、verified/accept 各有负向测试。 |
| P0 | GOV-02、RUN-01a：✅ 已关闭（commit 22b2ec6） | v1 contract approve 可绕过 Human Gate；Contract Gate payload 可引用另一项目 Contract，审批只按全局 contract ID 更新。 | 删除/封闭独立 approve 绕行；Decision、Gate、Contract、Project 在同一事务内校验并冻结；跨项目 target 和并发审批必须失败且无部分写。 |
| P0 | RUN-01、RUN-01b、TERM-01：✅ 已关闭（commit f94fac5） | claim 写入的 durable `runs.run_id` 未返回给 Runner，Runner另建 run ID；Terminal frame 只校验 generation，不校验 owner/token/当前 attempt，SSE 在首帧前可能锁定 job ID。 | claim 返回唯一 attempt run ID 并贯穿 Manifest/Terminal/Evidence；每帧校验 job/run/owner/generation/token；首帧前连接、retry 新 attempt、replay/gap/exit 和撤权均有真实 SSE 验收。 |
| P0 | RUN-02 ✅ 已关闭（commit 040e796）、RUN-01c ✅ 已关闭（commit f94fac5）、TEX-02 ✅ 已关闭（commit f94fac5：engine 固定 enum/命令数组 + TeX path 根内校验；UI 层实时 Terminal/PDF freshness 属 §4 行 95 P1） | TeX engine 与文件路径直接拼入 shell（已修复：TEX_ENGINES 固定 enum，非法 engine→422 tex_engine_invalid；TeX path root-relative、无 `..`、无 shell 元字符→否则 422 tex_path_invalid）；Metrics 路径可通过 `../` 逃出 outputs（已修复，realpath 必须位于 outputs）；默认 subprocess Runner 允许任意 smoke script 在宿主执行（已修复：subprocess 下未标记 `payload.trusted_fixture=true` 的 smoke job 拒绝执行，宿主 marker 负向断言证明未执行）。 | engine 固定 enum/命令数组，TeX path 严格字符与根内校验；metrics realpath 必须位于 outputs；smoke 默认容器，subprocess 仅允许显式 trusted-smoke-fixture（payload.trusted_fixture=true）；加入命令注入、路径穿越和宿主执行负向测试。关闭证据（源码修复+负向验收+当前提交证据）：tests/security/run-hardening-tests.sh 21/21（smoke 宿主执行负向：failed/environment + 宿主 marker 不存在；trusted fixture 正向 succeeded + metrics artifact）、run-release-bundle-tests.sh 16/16（docker 模式 smoke 不受影响）、run-fencing-tests.sh 15/15、tests/unit/tex-kernel.test.ts + malformed-path 测试（engine/path/realpath 负向）、pnpm vitest tests/unit/kernel.test.ts 97/97、node scripts/verify-docs.mjs 18/18；commit 040e796。 |
| P1 | TEX-01、TEX-02、TERM-01：部分——kernel/工作区层已闭环，UI 浏览器层剩余 | **kernel/工作区层已闭环（commit 64a0649）**：(1) snapshot 冻结可物化字节——tex_snapshot_files 按 revision 存 content+hash，GET /v1/documents/{id}/snapshot-files?revision=&path= 供 Runner 取冻结字节并逐文件 hash 校验；Runner 不再后取当前文件，不可读/篡改/逃逸一律硬失败；编译期间文件被修改，构建输入仍是冻结 revision 字节（负向断言）。(2) 保存失败立即终止编译——POST builds 冻结与提交两处 document-revision CAS，save 409（revision 前进）后旧 revision 编译 409 document_version_conflict，不创建 Job、不创建 build 行、不产出 PDF；carried manifest revision 与 tex_revision 不一致同拒。(3) dirty/clear/revert 与 CAS 覆盖——UI dirty 基线改为文件 GET/最近保存内容（tree 无 content，原实现清空非空文件 dirty=false 已修复；清空 ''→dirty=true、revert→false 单元测试）；清空='' 是 CAS 可见新 revision、过期 expected_version 一律 409。(4) UI 逻辑层接口准备——save-409 后 msCompile 立即终止；queued/running 期间 Compile 按钮禁用；build 卡片显示输入 revision（build.revision）、stale 标识（build.revision<document.revision）与 job_id→Terminal 跳转（SSE GET /v1/jobs/{job_id}/terminal）。证据：tests/unit/tex-workspace.test.ts 8/8、tex-build.test.ts 8/8（含 HTTP snapshot-files 与 save-409-compile 集成）、tex-kernel.test.ts 9/9（save-409 拒绝、manifest-revision 不一致、frozen-bytes）、manuscript-dirty.test.ts 4/4、kernel.test.ts 97/97、run-terminal-tests.sh 11/11、run-malformed-path-tests.sh 4/4、research-ui typecheck。**UI 浏览器层剩余（Playwright 类环境不可用，如实保留）**：Manuscript 页内嵌同一 Job 的 live stdout/stderr DOM（SSE 端点与 job_id 关联已就绪，页面内组件化与 DOM 有界验收待浏览器环境）；PDF freshness/stale 的浏览器展示验收；防重复按钮与 save-409 终止编译的浏览器验收；完整 log Artifact 下载入口与 build terminal 同页展示。 | snapshot 冻结可物化字节；保存失败立即终止编译；dirty/clear/revert 与 document revision CAS 覆盖；编译按钮防重复；UI 展示同一 Job 的 live stdout/stderr、完整 log Artifact、输入 revision 与 stale PDF。kernel/工作区层已按左侧证据闭环；浏览器层剩余项见“当前阻断”列。 |
| P1 | PTY-01、WORK-01、TEX-03、RUN-REMOTE-01、CONFIG-01：未实现/部分 | 当前只读 Terminal、TeX-only text store、轮询构建、本机 subprocess/docker 和分散配置，不能满足可操作研究工作台。**CONFIG-01 服务端全量覆盖已落地（commit f64690b + 009531c）**：canonical Config Registry（packages/research-schemas/src/config-registry.ts）——7 scope、45 键（覆盖 ExecutionConfig/IntegrityConfig 全字段、kernel/runner/orchestrator/standalone CLI、images.lock 与 network_policy、DSH_* env 别名），validateConfig（默认合并+未知键拒绝+security floor 违规拒绝，含 subprocess 模式非 none 容器网络）+ sha256 pin，parseCli 注册表 CLI 解析已接入四个二进制（--help 生成），生成 JSON Schema/template/CLI help（configs/generated/），运行中对象 pin hash（kernel.configPinHash+configRedacted + x-config-pin 响应头 + health config_pin + GET /v1/config/effective（redacted）+ GET /v1/config/schema + endpoint 文件 configPin + runner 启动 pin），createProject 项目级 registry 校验；证据 tests/unit/config-registry.test.ts 30/30、kernel.test.ts CONFIG-01 组 5/5、pnpm test 全绿、research-schemas/research-kernel/runner-gateway/research-orchestrator/research-ui build 全绿、run-hardening/run-fencing 不破坏、verify-docs 18/18（详见 §3 CONFIG-01 行）。剩余（浏览器层，Playwright 类环境不可用，如实保留）：Settings UI 由 /v1/config/schema + /v1/config/effective 生成（/bff/research/config/* 随 Settings UI 落地）、job scope 键、SecretRef 存储层，属后续阶段。**TEX-03 kernel/逻辑层已闭环（commit 1907897）**：save-success hook POST /v1/documents/{id}/preview-builds（server 端 debounce 默认 800ms 可配置 + 持久化 tex_preview_pending + Kernel 定时器，重启自动重挂）+ preview build 状态机（queued→running→succeeded/failed；新 revision/权威 Compile 到达时 queued→cancelled、running→superseded，superseded_by/at，job 尽力取消，终态不被完成事件改写）+ preview 复用同一固定 texlive image/禁网/no-shell-escape latex-compile runner 路径（payload.preview=true，不产 accepted Evidence、不冻结/不参与权威 manifest 链）+ stale 判定（build.revision<document.revision→build 记录 stale=true）+ 权威 Compile 创建即 supersede 全部非终态 preview（活跃权威编译期间 preview flush 跳过，preview 永不阻塞/取代权威 Job）+ claim 时 build queued→running、cancelJob 同步 build + migration 0010（tex_builds preview/superseded_by/superseded_at + tex_preview_pending，SCHEMA_VERSION 9，幂等）。证据：tests/unit/tex-preview.test.ts 12/12（debounce 合并、取消 queued、running→superseded、stale、权威 supersede、preview 不产 Evidence、同 revision 去重、重启持久化、previewAutoTrigger、HTTP 路由）、tex-kernel.test.ts 9/9、tex-build.test.ts 8/8、tex-workspace.test.ts 8/8、kernel.test.ts 100/100、migrations.test.ts 8/8、pnpm vitest 全绿、latex-compile-e2e 13/13 不破坏、node scripts/verify-docs.mjs 18/18（详见 §3 TEX-03 行）。**剩余（浏览器层，Playwright 类环境不可用，如实保留）**：debounce 后自动出 PDF 展示、stale 标识 UI、live log/diagnostics/PDF 同页展示、preview 面板与权威 build 面板分离的浏览器验收。**PTY-01/WORK-01 深 Interface/Schema/migrations 已推进（commit aeb71c9）**：PTY-01——research-schemas `pty.ts`（PtySession 固定 principal/project/workspace/profile/target/preset/relative cwd/config_hash/lease；PtyControlFrame client_seq 幂等键；PtyOutputFrame server_seq output/exit/gap + retention 语义）+ kernel `pty-session.ts` 状态机（open→attached→detached→closed；attach/detach generation 递增；权限撤销→detach；idle TTL/lease/显式 close→closed；client_seq 重复回放幂等/乱序 409 pty_client_seq_out_of_order；输出 server_seq 单调、retention_bytes 有界淘汰先发 gap）+ PtyAdapter 接口与 NullPtyAdapter（LocalDockerPty/RemoteRunnerPty 挂载点，spawn 失败 adapter_failed 关闭并保留审计行）+ HTTP /v1/pty/* schema 校验层（POST sessions 校验后 501 pty_adapter_not_implemented 不创建惰性会话；control/frames 在 kernel-created 会话上完整可用，delivered=false 如实标注；404/422 fail-closed）+ 迁移 0011（pty_sessions/pty_frames，SCHEMA_VERSION 10，幂等）；PTY 输出永不进入 Metrics/Manifest/Evidence/Gate。WORK-01——research-schemas `workspace.ts`（WorkspaceNode path/kind/media/size/revision/etag/hash、WorkspaceOp create/write/delete/move target version CAS）+ kernel `workspace-store.ts`（list/read/write/delete/move + revision/etag 语义、预期 version/etag 冲突 409、create-if-absent、二进制 CAS 复用 artifact CAS blob_sha256、文本写二进制 422 read-only、路径安全 snapshot-walk 契约、move 目标冲突 409、op ledger history、dir 前缀投影）+ `tex-facade.ts`（TexWorkspaceFacade 映射 TeX store 到同一 WorkspaceStoreLike 契约，TeX 作为 facade，无第二套 bytes/revision 权威）+ 迁移 0011（workspaces/workspace_nodes/workspace_ops）。证据：tests/unit/pty-session.test.ts 13/13、tests/unit/workspace-store.test.ts 8/8、migrations.test.ts 8/8、pnpm vitest 全绿、research-schemas/research-kernel build 全绿（详见 §3 PTY-01/WORK-01 行）。**剩余（adapter/UI 轮）**：真实 PTY adapter 与远程 wire、WebSocket 双向 attach、浏览器 TUI 验收、workspace 文件系统 adapter 与 tabs/search/watch/upload/move/history/Problems UI、桌面/窄屏/冲突/路径/二进制浏览器验收。**RUN-REMOTE-01 深 Interface/Schema 阶段已推进（commit aeb71c9）**：ExecutionTarget port + LocalDockerAdapter（docker 执行路径收敛到 port，plan 校验/冻结/fingerprint 不可变断言，buildLocalDockerArgs 纯参数映射与既有容器基线一致）+ ExecutionPlan schema（project/job/run/lease/profile/config/image digest/snapshot/artifact refs/limits/network/output contract + Ed25519 签名与 payload_sha256 复算，`.strict()` 拒绝连接信息字段）+ RemoteAgentRegistration + scheduledTarget 纯函数（capability 匹配、offline/draining 拒绝、无匹配→明确 retryable；bound target 不静默回退 LocalDocker/subprocess，policy 可配置）+ InMemoryAgentRegistry（注册/心跳/offline 判定）+ RemoteRunnerAgent fail-closed stub（真实 mTLS 未实现，执行调用明确抛错）；subprocess 明确为非 target 兼容层（trusted-smoke-fixture 专用）。证据：tests/unit/execution-target.test.ts 12/12、tests/unit/remote-fleet.test.ts 23/23、pnpm vitest 新增两文件 35/35、research-schemas/runner-gateway build 全绿（详见 §3 RUN-REMOTE-01 行）。**wire 协议 + 服务端/代理端已实现（commit 009531c）**：RemoteFleetServer + RemoteRunnerAgentImpl + AgentOutboundSpool + mock/HTTP 传输，tests/unit/remote-wire.test.ts 15/15、根 pnpm test 全绿（详见 §3 RUN-REMOTE-01 行与 docs/acceptance-tests.md §4.2）。剩余：真实 mTLS 证书链与真实远端 sandbox 验收、跨主机网络分区故障注入、Remote PTY、浏览器 UI。**PTY-01 LocalPtyAdapter 已实现（commit f54bfe3）**：真实 pseudo-terminal（python3 pty.fork 桥，pty-local.ts）——preset→argv 白名单 + cwd 相对 workspace 根（adapter 层重校验）+ env 白名单（DSH token/service token/模型凭据/host $HOME 不可达，HOME 重定向进 pty-workspaces sandbox）；bytes input/resize（前台 pgrp 信号 INT/TERM/KILL 真实终端语义）/detach 不杀进程（输出继续入 store，reconnect generation+after_seq 重放）/显式 close 与 idle TTL（kernel 30s sweep）真实拆 tty 无孤儿；HTTP /v1/pty/sessions 在注册 adapter 后由 501 改为真实 open（x-principal-id + membership fail-closed，201 真实会话；无 adapter 仍 501）；kernel bin 默认注册，standalone/plugin sidecar 同路径获得真实 PTY。证据：tests/unit/pty-local.test.ts 11/11、pty-session.test.ts 13/13 不破坏、research-kernel build 全绿、verify-docs 18/18（详见 §3 PTY-01 行）。剩余：Remote 传输与浏览器 TUI（LocalDockerPty/RemoteRunnerPty 共享 wire、WebSocket 双向 attach、真实终端渲染浏览器验收）；idle TTL/retention 的 Config Schema registry 键。**WORK-01 磁盘 adapter 已实现（commit e298cee）**：真实磁盘存储——每项目树根 `dataDir/workspaces/{project_id}/{workspace_id}/`（0750 链/0640 文件），节点字节落盘、`workspace_nodes` 只存元数据；原子写（临时文件+rename）；revision/version/etag 单调；二进制节点（multipart assets ≤32 MiB 服务端 sha256，blob_sha256 注册 artifact CAS 幂等引用，工作字节以树文件为准）；大小上限 32 MiB→413；move/delete CAS（409）+ 原子 rename；路径安全（规范化负向 + 任意 symlink 拒绝 422 workspace_symlink）；history（每路径最近 8 版字节于树外 .ws-meta，readVersion 回退读）；listSince/watch + 路径 search（内容搜索未实现）；server `/v1/projects/{id}/workspaces*` 全路由 + BFF multipart 原字节透传与鉴权保持；TeX facade 对齐（listSince 保守整树、readVersion 仅当前版本）。证据：tests/unit/workspace-store.test.ts 15/15、tests/security/run-workspace-tests.sh 38/38（已接入聚合器）、pnpm vitest 全绿、research-schemas/research-kernel build 全绿、verify-docs 18/18（详见 §3 WORK-01 行）。剩余：浏览器编辑器 UI（tabs/search/watch/upload、move/history 面板、Problems、集成 PTY）与桌面/窄屏/冲突/路径/二进制浏览器验收（Playwright 类环境不可用）；远程文件系统 adapter。 | 先完成深 Interface/Schema/migrations，再完成 Local/Remote adapter 与 UI；阻断 acceptance 的 workspace/pty/remote/config/live-preview 全部零 SKIP。 |
| P1 | ONBOARD-01、UPLOAD-01、GUIDE-01：未实现/部分 | 无法安全接入外部已完成研究，普通 upload 会丢 provenance/阶段语义；用户下一步只是字符串提示。**GUIDE-01 kernel 层部分闭环（commit f64690b）**：结构化 NextAction 已实现——`next_actions_v2: NextAction[]`（code/label/reason/required/route/capability/revision/state + blocking/refs/required_by）由 `nextActionProjection` 纯函数按状态机每阶段投影，legacy `next_actions: string[]` 由非 done 动作 label 稳定派生，未知/未来状态退化 `code='unknown'` 不抛错，pending gate（含 budget 阻塞）与失败作业 retry/repair 动作齐备；证据 tests/unit/next-action.test.ts 10/10、run-gate-tests.sh 15/15、verify-docs 18/18。剩余：Intake/Grill 阶段动作在 ONBOARD-01 落地前无意义（如实保留），每阶段/失败/Gate 浏览器验收待 Playwright 类环境。**UPLOAD-01 服务端/BFF 层已闭环（commit 1907897）**：kernel 原生 `POST /v1/projects/{id}/uploads` multipart 端点（≤32 MiB 流式强制 413、服务端 sha256 绑定、路径安全 422、staged→finalize 原子+回滚、同 project+hash+filename 幂等 200 reused:true、cleanupStagedUploads GC）+ standalone BFF 原始字节/原 boundary 透传（bearer/Origin/membership/role 检查与其它 /v1 写一致）；证据 tests/unit/upload.test.ts 20/20、tests/security/run-upload-tests.sh 18/18（curl -F 上传/BFF 透传/大小负向，已接入聚合器）、pnpm test 437/437、两个包 build 全绿（详见 §3 UPLOAD-01 行）。**ONBOARD-01 kernel/服务端层已实现（commit f54bfe3）**：Intake 会话全链（begin→stage→scan→grill→propose→adopt/reject，research-onboarding.md §3 状态机）——research-schemas `intake.ts`（IntakeSession/IntakeArtifact/IntakeObservation(trust=observed_unverified)/GrillQuestion+Answer(human_assertion)/PhaseProposal/AdoptionReceipt/IntakeProjection）+ kernel `intake.ts`（确定性 taxonomy v1（11 稳定 code，按 target_phase 裁剪 required）、静态扫描（扩展名白名单/可执行 deny、magic、ELF/PE、静态 secret 模式且值永不回显、av_available=false 如实、archive 解包留给采用时 walk）、SAFE_PHASE_LANDING §6 表）+ kernel.ts 方法 + migration 0012（SCHEMA_VERSION 11）+ server.ts `/v1/projects/{id}/intake*`（multipart stage ≤32MiB 复用 UPLOAD-01 上限、scan/questions/answers/propose/adopt/reject、跨项目 404、answers/adopt/reject principal fail-closed 422 principal_required）；**pre-accept 零权威写**（begin/stage/scan/answers/propose 只写 intake_* 表 + 隔离 staging CAS（intake-staged/<iid>/<sha256>.part），业务表与 Outbox 零写——测试断言 gates/decisions/artifacts/jobs/runs/evidence/claims/terminal/workspaces/tex 计数 0 + CAS 空）；**adopt 单事务**：blobs→CAS+Artifact 行、阶段 Gate 只创建 pending（绝不伪造 IDEA_APPROVED/CONTRACT_APPROVED/RELEASED）、metrics→draft legacy_unverified Evidence、log→ImportedRunObservation（绝不 TerminalLog）、AdoptionReceipt 钉 proposal/target revision（409 proposal_stale/project_revision_conflict）、同 Idempotency-Key+hash 同 Receipt（异 hash 409）；expireIntakes（7 天）+ cleanupIntakeStaged（24h）GC、reject 清理；恢复：GET 投影完整可续（重启后 answers/proposal 完整）；提案 next_actions 复用 GUIDE-01 NextAction schema（投影 Intake 覆盖动作留待后续轮次，本轮不动 next-action 已提交代码）。证据：tests/unit/intake.test.ts 32/32（全链、答案不齐 422、零权威写、幂等、恢复、GC、reject、principal、扫描规则、HTTP+跨项目 404）、migrations.test.ts 8/8、pnpm vitest（排除并行子代理 in-flight 的 pty-local.test.ts 后 526/526）、research-schemas/research-kernel build 全绿、node scripts/verify-docs.mjs 18/18（详见 §3 ONBOARD-01 行）。**剩余（UI/浏览器与后续轮次，如实记录）**：拖拽/向导 Intake UI、quarantine/scan 进度、分块 offset/hash 恢复上传与研究包 archive 解包扫描、TeX workspace/CodeSnapshot 采用物化、merge 冲突交互、v2/BFF accept 面与 Agent tool 面、浏览器验收（Playwright 类环境不可用）。 | Intake quarantine/scan/Grill/Proposal/Human Adoption、结构化 NextAction、上传恢复/冲突/GC 和 Gate/Run/Evidence 防伪全部实现。**ONBOARD-01 kernel/服务端层（Intake 全链 + 静态 scan + 确定性 Grill taxonomy + PhaseProposal + Human Adoption（principal/revision 钉定/幂等）+ reject/expiry/GC/恢复 + pre-accept 零权威写 + Gate/Run/Evidence 防伪断言 + 提案 next_actions 复用 NextAction schema（投影 Intake 覆盖动作留待后续轮次））已按左侧证据闭环（commit f54bfe3，tests/unit/intake.test.ts 32/32）**；**UPLOAD-01 服务端/BFF 层（multipart ≤32MiB、hash/路径/幂等/GC/回滚）已按左侧证据闭环**；剩余为 UI 浏览器层（拖拽上传、intake 向导/quarantine/scan 展示、上传恢复/冲突 UI，Playwright 类环境不可用如实保留）与后续轮次（分块 intake staged upload、archive 解包扫描、TeX workspace/CodeSnapshot 物化、merge 冲突交互、v2/BFF accept 面、Agent tool 面）。 |
| P1 | TRAJ-01、SUBAGENT-01：部分——**standalone 投影与拓扑 API 层已实现（commit e298cee）**，浏览器拓扑渲染/进入 child 与 10k node 浏览器验收剩余；UI-SIMPLE-01：逻辑层已闭环（commit 1907897），浏览器层剩余 | DSH Web 轨迹/子代理目录未移植到浏览器，页面不能看到拓扑或进入 child。**standalone safe trajectory projection 与 child 拓扑 API 层已实现**：(1) 只读投影——`GET /v1/projects/{id}/trajectory`（`(event_seq,event_id)` keyset 分页、单页 ≤500、total/has_more、`lane=research|session` 过滤）与 `GET .../trajectory-lanes`（Research vs Session 双泳道、各自游标）；投影只含白名单 redacted summary（token/secret/绝对路径 scrub、statement ≤240 截断、无 raw payload），research 泳道=权威业务事件、session 泳道=observational 会话/子代理事件；(2) child 拓扑——`child_links`/`child_history`/`child_followups`（migration 0013，SCHEMA_VERSION 12）+ `GET /v1/projects/{id}/topology?parent_id=`（exact direct children、has_children/children_count/seq 游标）、`GET /v1/topology/{child_id}`（exact-parent + breadcrumb，cycle-safe/orphan fail-soft/深度 32）、`GET .../history`（只读，started/registered/state/followup 账本，不激活 Agent）、`POST .../followup`（one-shot 只读：记录 message 返回 message_id、read_only=true、state_unchanged=true、不冒充已执行）、`POST /v1/projects/{id}/topology/children`（注册 child，re-register 不复活终态）+ `PATCH /v1/topology/{child_id}/state`（ended_at 首次终态钉定）；outbox 新增 `trajectory.child.started/updated/followup`（session 泳道）；(3) 权限——trajectory/topology 全部路由 principal+membership fail-closed（缺 x-principal-id 422 principal_required、非成员/未知 child 404 无枚举），BFF 先解析 child→project 再查成员并注入 x-principal-id；(4) research-client 方法齐备（projectTrajectory/projectTopology/childDetail/childHistory/childFollowup/registerChildLink/updateChildState）。证据：tests/unit/trajectory.test.ts 12/12（跨 bucket 平局分页续传、脱敏断言、10k 事件 21 页×500 不丢不重 <10s、tree/breadcrumb/exact-parent/orphan/cycle、history/followup 只读、HTTP 权限矩阵）、migrations.test.ts 8/8、pnpm test 555/555、research-schemas/research-kernel/research-client build 全绿、research-ui typecheck+build 全绿、node scripts/verify-docs.mjs 18/18。**UI-SIMPLE-01 逻辑层已闭环**：Start 三卡（nav.ts startActions()：new-project/open-project/import，首屏空项目列表时渲染，import 如实占位）、四 primary tabs + More（nav.ts tabGroups() 全覆盖 + 稳定深链 `#tab=<key>`/`#settings` parseDeepLink + navOrder() Alt 序 + TAB_IDS 对齐 ALL_TAB_KEYS）、Settings 渐进披露（settings-model.ts settingsSections() 九组 Accordion 默认折叠 + config provenance pin 显示/占位）；证据：tests/unit/ui-simple.test.ts 12/12、i18n-chrome.test.ts 3/3、i18n-runtime.test.ts 12/12、research-ui typecheck+build 全绿、根 pnpm test 全绿（详见 §3 UI-SIMPLE-01 行）。**剩余（如实记录）**：浏览器拓扑渲染/进入 child/breadcrumb 导航、trajectory/topology SSE 实时流（after_seq replay/gap/revoke）、10k node 浏览器 DOM 虚拟化与键盘/ARIA/zh-en 验收（Playwright 类环境不可用，需浏览器轮）、research_panel 插件侧 registerChildLink/updateChildState 接线（本轮未动 plugin 代码，kernel/服务端已就绪）、token 四桶/cost/permissions/retention 详情（需 DSH session adapter）。 | standalone safe trajectory projection、topology enter/breadcrumb/exact-parent/redaction、四 primary tabs + More + 折叠 Settings、i18n/a11y/10k node 验收。standalone 投影与拓扑 API 层已按左侧证据闭环；剩余为浏览器渲染/SSE 实时流/10k node DOM 虚拟化/a11y 与插件接线（浏览器轮）。UI-SIMPLE-01 剩余仅浏览器级：640/720/1024 视口、键盘导航/a11y、Start/More/Accordion 的 DOM 有界断言（Playwright 类环境不可用，如实保留）。 |
| P1 | UI-02、UI-03：逻辑层已闭环（commit f64690b），浏览器层剩余 | **逻辑层闭环**：Terminal 元数据/exit 硬编码英文改 terminal.meta/exitLine 键（zh/en 含 dropped/truncated/exit 片段）；状态 pill/sidebar/search 状态文案改 status 命名空间 statusLabel()（en 保持 kernel enum 镜像、zh 翻译、unknown enum 原样显示）；tab/pipeline/header/modal 不再初始化时快照——i18n/chrome.ts 纯 chrome 模型渲染时求值，setLocale 重绘静态 chrome + overlay registry 重开已开 modal + panels/Terminal/状态 pill 重渲染；t() 缺 key（全字典缺失）返回 raw key、dev console.warn 一次/键（含字典路径提示）+ setMissingKeyReporter 注入收集器。证据：tests/unit/i18n-runtime.test.ts 12/12、i18n-chrome.test.ts 3/3、research-ui typecheck/build 全绿、根 pnpm test 全绿（详见 §3 UI-02/UI-03 行）。**剩余**：zh/en 浏览器测试（运行时切换的一个 render tick DOM 断言、缺 key 告警的浏览器 console、键盘与窄屏）需 Playwright 类环境，未验收。 | 所有已知 UI chrome 使用 key；locale 切换重建 tab、pipeline、modal、Terminal 与状态；zh/en 浏览器测试覆盖运行时切换、缺 key 告警、键盘和窄屏。 |
| P1 | DSH-01、SIDE-01：✅ 已关闭（commit 64a0649） | （原阻断）plugin 在 `sidecar.start()` 前读取 endpoint，`port=0` 直接抛错；tool/listener disposer 未收集，tool context 使用全局 mutable ref，多实例或重载会串线/泄漏。 | 关闭证据（源码修复+负向验收+当前提交证据，commit 64a0649）：(1) apply 改为 async 且被 Cordis 4.0.0-rc.7 await（fiber `_execute` 收集 thenable apply 结果、`ctx.plugin()` 经 `fiber.await()` 等待 apply 落定；已核对 cordis 源码，未改框架），先 `await sidecar.start()` 再构造 client、`ctx.provide('research',{endpoint: sidecar.endpoint,…})` 并注册工具/命令/skills——port=0 时 endpoint 恒为启动后解析的真实端口，任何工具/命令都不会在 kernel 就绪前拿到未解析值；start 失败明确日志+rethrow（fiber FAILED 并卸载已注册效果，无半初始化资源）；sidecar disposer 在 apply 开头注册，启动期间 dispose/reload 也能停掉 kernel。(2) 全部资源纳入 Cordis disposer：`ctx.tools.register`/`ctx.commands.register`（dsh-scope ScopedLayers→ctx.effect）、`ctx.on`/`ctx.provide`/`ctx.plugin`（cordis fiber effect）与 sidecar `ctx.effect` 均随插件 fiber 卸载自动回收；reload（cordis `update()`）先卸载旧 fiber（旧 kernel 停止、工具/命令/skills/监听器全部回收）再重新 apply，无重复注册/监听器残留。(3) 实例闭包：删除模块级 `toolContextRef`，`researchTool(def, toolCtx)` 在注册时把本实例 client/cache/roles/ctx 绑定进 execute 闭包，多实例互不串线。(4) 真实 host fixture：完整 DSH 宿主 boot 在本环境不可用，改用**最小 Cordis host fixture**（真实 cordis Context + 真实 ToolRegistry/CommandService/SkillService，仅 stub systemPrompt/subagents，如实记录）：port=0（endpoint=0600 endpoint.json 真实端口、client 真实 kernel create/list 调用）、双实例（endpoint/kernel/roles/ACL/工具 client 全隔离，B 实例授权不泄漏进 A）、reload（旧 kernel 终止、工具/skills/pre-execute 监听器不重复、同 dataDir 数据保留）、dispose（sidecar 停、端口释放、owned endpoint.json 清理、tools/commands/skills/监听器全回收、兄弟实例不受影响、同 root 重载可用且恰好注册一次）。证据：tests/security/run-dsh-plugin-tests.sh 47/47（原 41 + host fixture 组 6 断言：async apply/port=0、双实例隔离、reload、dispose、re-apply 全 PASS）、tests/unit/sidecar.test.ts 8/8（SIDE-01 既有用例未破坏）、pnpm --filter @dsh-scholar/research-plugin run build、pnpm test 348/348、node scripts/verify-docs.mjs 18/18。剩余：真实 DSH 宿主完整 boot 全链验收（CI-01 外部 host 环境）。 |
| P1 | REL-01：✅ 已关闭（commit 040e796） | bundle reproduce 仍读取外部 `KERNEL_BIN/RUNNER_BIN`；clean-room 复用原 `$WORK/code` 与旧 CAS，比较只覆盖浅层数量/存在性。 | 空目录、无原 checkout/DB/CAS/网络下仅从 Bundle 重放；外部 runtime/path 读取立即失败；逐字段比较 manifest、metrics、analysis、RunManifest、TeX 输入与 PDF 结构。关闭证据（commit 040e796）：(1) 空目录、无原 checkout/DB/CAS：reproduce.sh 把 bundle 复制进全新 mktemp 目录，kernel 以全新 `--db/--cas` 启动；报告 `cleanroom{snapshot_dir,kernel_db,kernel_cas,work_dir}` 证明均在新建临时目录、不含原 checkout/bundle 路径；负向用例把原 checkout 目录改名后仅以改名后的 runtime 树重放仍 status=pass（任何对原路径的读取都会失败）；网络面=仅 loopback HTTP + docker `--network none`（既有 RUN-02 容器基线）；(2) 外部 runtime/path 立即失败：指向原 checkout 且 digest 不匹配→preflight 拒绝（external checkout access prohibited）；checkout 外 digest 不匹配二进制→preflight 拒绝（sha256 do not match），均不启动 kernel/runner 并写 status=fail+compared 全 false 的 fail report；(3) 逐字段比较：manifest（snapshot sha256 vs 复制前原 hash）、metrics（name/unit/value 数值容差+seed）、analysis（mean 容差+n/effect_size/baseline_value）、RunManifest（idempotency-key 集合+数量+每 key kind+run_manifest/metrics_artifact 存在性）、TeX 输入（文件清单+每文件 sha256，bundle 快照 vs 重建 document）、PDF 结构（字节大小相等）；报告含 tex_comparison 明细（inputs.files_matched、pdf.size_matched）。测试：tests/security/run-release-bundle-tests.sh 25/25（断言 8-12：tex 清单/hash+pdf 大小、tex_comparison/cleanroom 字段、checkout 改名重放、digest 拒绝、node 版本不匹配→status=fail）、run-release-eval.sh 25/25（含真实 texlive latex-compile 段，reproduce 段 pass）；commit 040e796，尚未绑定 CI job 报告。 |
| P1 | CI-01、DOC-01、DOC-02：部分 | `ci-gate --skip-security` 与 LaTeX 缺依赖会 `SKIP+exit 0`；fencing 套件把不存在的 Artifact finalize（404）计为 PASS；当前 shell 无 `pnpm`，DSH 插件脚本 37/41，且把 `npm pack` prepare 失败误报成四个 skill 缺失。 | CI 必跑项零 SKIP，缺依赖非零；不存在的必需 route 必须 FAIL；先断言 probe/pack 命令成功再判断产物；当前 commit 在完整工具链重跑全部矩阵并绑定机器可读报告。 |

额外阻断：Runner `completeJob(status=succeeded)` 在缺失 RunManifest 时仍可成功，Job 与 Run 更新也不在同一事务；正式 Analysis 对重复 seed 静默覆盖且统计公式与规范 golden vector 不一致；draft/unverified Evidence 可进入 Manuscript。三项分别归入 RUN-01、STAT-01/STAT-01b、EVID-01，在关闭上述 P0 前不得宣称正式研究链可用。**关闭状态：三项均已关闭**——RUN-01 于 commit f94fac5（claim 返回 durable run_id、completeJob 缺 manifest 422 run_manifest_required、Job+Run 同事务、重复 seed 422 duplicate_seed）；STAT-01/STAT-01b 于 commit 22b2ec6（公式按 §12 重写，golden vector 重生成一致，独立 canonical-RNG-rule 镜像重算相等，run-analysis-spec-tests.sh 29/29）；EVID-01 于 commit 22b2ec6（draft/unverified Evidence 不进 Manuscript，claims_used=0 且语句缺失，spec 测试断言；verified→accepted 链 claims_used=1）。

本轮只读验证：`node scripts/verify-docs.mjs` 为 16/16；`bash tests/security/run-fencing-tests.sh` 报 12/12，但包含上述 404 假阳性；`bash tests/security/run-dsh-plugin-tests.sh` 为 37 passed/4 failed，根因为当前环境缺 `pnpm` 导致 `npm pack` prepare 失败后被脚本误诊；`pnpm test` 未运行（`pnpm: command not found`）。这些结果都不能升级状态。

## 5. 2026-08-09 审阅证据

审阅快照：`main@7adc722`，当时与 `origin/main` 一致且工作区干净。

| 检查 | 结果 | 解释 |
|---|---|---|
| `node scripts/verify-docs.mjs` | 通过，16 documents | 仅证明结构、链接、片段和删除面静态检查，不证明规范语义或实现验收 |
| `node scripts/verify-docs.mjs --diff-check definitely_missing_ref` | 错误地退出 0 | base ref 不存在时吞掉 git 错误，DOC-02 fail-open |
| `bash tests/security/run-standalone-http-tests.sh` | 43/43 | 证明局部 token/Origin/binary/404/membership；SSE 只验 status，未证明 frame/replay/撤权，未覆盖 CSRF token |
| `bash tests/security/run-all-v2-blocking-tests.sh` | 汇总 7 个 PASS | lower-is-better 实际 SKIP exit 0，被聚合器计为 PASS；不能作为零 SKIP 验收 |
| `bash tests/security/run-release-bundle-tests.sh` | 0 passed, 2 failed | analysis 阶段退出，未产生 `BUNDLE_DIR`；REL-01 当前阻断 |
| UI typecheck/full test | 本轮未完整重跑 | 当前审阅 shell 无 `pnpm`；不得继承旧 Playwright/计数为当前证据 |

上表是审阅基线（`main@7adc722`）时点的证据快照，只描述当时观察。基线后本会话的变更与新增证据以 §3 矩阵行（尤其 RUN-01a/b/c、STAT-01a、CI-01a、RUN-02）的“当前阻断与关闭条件”为准；与上表冲突处（如聚合器对 SKIP 的处理、lower-is-better 的启用状态）以后者为准。

## 6. 后续执行硬顺序

以下不是建议顺序，而是关闭状态的依赖顺序。前一批的阻断场景未通过时，不得把后一批宣称为完成或发布就绪：

1. **P0 Governance**：Principal、tenant、membership/role AuthZ、Human Gate、CSRF、service identity；
2. **P0 Formal execution**：approved Contract、immutable snapshot/data、fixed image digest、strict lease/Run/Manifest、MetricsFile；
3. **P0 Evidence/Release**：verified→accepted、Claim only accepted、Analysis provenance、bundle-only clean-room；
4. **P1 Product surfaces**：Workspace/PTY/Remote Runner/Config、Terminal 完整日志、LaTeX live preview、Init/Upload/Grill/NextAction、Trajectory/Subagent topology、简洁导航与全页面 i18n；
5. **P1 DSH/package**：实例闭包/disposer、sidecar handshake、canonical tools/commands、Skill 选择、clean install、自修改隔离验收；
6. **P1 CI/docs**：零 SKIP、root/full/browser/DSH/release jobs、diff base fail-closed、文档语义一致；
7. **Final validation**：当前提交运行全部阻断命令、Golden Path、100 次 recovery、bundle-only clean-room，并绑定机器可读证据。

任一 P0 为“未实现/部分/已实现未验收”，产品只能保持 Security Alpha，不得用于无人值守正式研究或公开发布。

## 7. 文档与状态更新规则

每个实现或修复变更必须在同一变更集内：

1. 更新负责行为的规范；
2. 更新 `acceptance-tests.md` 的自动化场景；
3. 更新本矩阵的“当前阻断与关闭条件”，不能只改状态词；
4. 若用户操作变化，更新 `USAGE_GUIDE.md`；
5. 记录当前 commit/CI/报告证据，或明确写“未验收”；
6. CI 阻断 job 零 SKIP；本地允许的显式 skip 不计入 PASS；
7. README、USAGE、acceptance、repository blueprint 与本文件有矛盾时，合并与完成声明均阻断。

只有对应 acceptance 场景在当前提交和目标 CI 环境全部通过，状态才能从“部分”进入“已实现未验收”，再进入“已验收”。修复代码但未补规范、验收或状态，视为未完成。

本次 2.3 规范更新已运行 `node scripts/verify-docs.mjs`（18 documents）与 `git diff --check`；它只证明 Markdown 结构、链接和约束片段一致。PTY、通用 Workspace、实时 Preview、Remote Runner、Config Registry、Onboarding/Upload/Grill/NextAction、Trajectory/Subagent Topology 和简洁 UI 的实现状态仍按 §3 保持“未实现/部分”。

## 8. 最终差距审计轮（2026-08，未绑定 CI job，全部“已实现未验收”）

本轮为收尾审计：逐篇对照 docs/ 规范性文档与代码实现，消灭“可实现但未实现”差距。审计报告：/tmp/final-audit-report.md。本轮改动（审计后随附实现轮已关闭剩余两大项 OBS-01 与 STORE-05，见下表对应行，commit 849a788）：

| ID | 目标 | 当前状态 | 本轮关闭/剩余 |
|---|---|---|---|
| ORCH-01 | Orchestrator §15 选主（orchestrator_leases）与 CLI 全参数 | 已实现未验收 | reconstruction-contracts.md §15 要求的 --owner/--lease-seconds/--token-file 与 orchestrator_leases(project_id,owner,generation,expires_at) 已实现：ActionStore 新增 leases 表与 claimLease（首占/同 owner 续约 generation+1/过期抢占/他人 live lease 拒绝）/refreshLease/releaseLease/leaseHolder；Engine 每轮 claim、他人持有则跳过该项目（detail.skipped，不静默双推进）、每轮 refresh、close() 释放全部租约；--token-file 读取 0600 kernel bearer token 附 Authorization 头（缺失文件 fail fast）；owner 缺省 orch-<hostname>-<pid>；Config Registry orchestrator scope 新增 3 键并重新生成 configs/generated/（cli-help/schema/template）。证据：tests/unit/orchestrator.test.ts 41/41（新增选主 4 用例：抢占/续约/过期接管、refresh/release、双实例跳过与接管、Authorization 头与缺失文件 fail fast）、根 pnpm test 586/586。剩余：多实例真实进程并发验收、CI job 绑定。 |
| V2-HEALTH | /v2/health 规范 HealthResponse（reconstruction-contracts.md §5 / api-contracts.md §3） | 已实现未验收 | /v2/health 改为规范形状：protocol_version='v2'（字符串）、capabilities 对象含 14 项服务端能力布尔（terminal_stream/interactive_terminal/workspace_files/tex_workspace/latex_compile/latex_live_preview/remote_runner/config_registry/research_onboarding/trajectory/subagent_topology/signed_manifest/clean_room）+ locales ['zh','en'] + locale_contract_revision 1，保留 config_pin。证据：tests/unit/v2.test.ts 6/6（断言更新为规范形状）。剩余：浏览器/外部客户端对该形状的消费验收、CI job 绑定。 |
| OBS-01 | §18 可观测性 /internal/metrics | 已实现未验收 | 结构化 JSON 日志字段已落地；`/internal/metrics` 端点与指标计数已实现（commit 849a788）：kernel 内存 MetricsStore（`packages/research-kernel/src/metrics.ts`，counter/histogram 极简实现，`count(key, tags?, delta)`/`observe(key, value, tags?)`/snapshot JSON）在关键路径打点——HTTP request 计数+延迟直方图（server 层 response finish 记录，method/status 常量 tag）、outbox append（emit）/dead-letter（新增 kernel `deadLetterEvent`，§16 消费者侧账务 + `outbox.dead_letter` 计数）、job claim/complete（`job.claimed`/`job.completed{status}`）、lease expiry（recoverExpiredLeases 累计回收数）、terminal dropped bytes（appendTerminalFrames 累计淘汰字节）、CAS orphan GC（collectOrphanBlobs 累计清除数）、TeX build 完成（texUpdateBuild 终态 `tex.build_completed{status}`）、budget 记账（recordUsage `budget.recorded` + `budget.model_cost_usd` 直方图）；server `GET /internal/metrics` 仅 loopback 可达（remoteAddress ∈ 127.0.0.1/::1/::ffff:127.0.0.1 或 bind host loopback；非 loopback 403 `loopback_only`），JSON 输出、不要求 service token（与 /v1/health 同级公开面）；connector 失败计数经 `multiSourceSearch` 可选 metrics 观察者（`connector.source_failure{source}`，结构性 duck-typing 无包依赖）；快照只含固定常量 key/tag 与数值，不含 token/路径/内容。证据：tests/unit/metrics.test.ts 18/18（计数/直方图/delta/打点路径/端点 loopback 200 与 403 与无 token/敏感字段卫生/connector 观察者）、根 pnpm test 648/648、run-terminal-tests.sh 11/11、run-sse-tests.sh 23/23、verify-docs 18/18。剩余：OpenTelemetry adapter 留作未来 export 面（设计如此）、CI job 绑定。 |
| STORE-05 | storage-migrations.md §4 terminal_frames 内容完整性 | 已实现未验收 | 相同 (run_id, seq) 内容不同 → 409 `terminal_frame_conflict`（不再 INSERT OR IGNORE 静默）：appendTerminalFrames 对 seq ≤ cursor 的重放帧按 (job_id, run_id, seq) SELECT 比对内容签名（frame_kind/stream_seq/channel/text/byte_offset/byte_length/payload_json——lease_generation/created_at 属账务不算内容，sha256 规范化签名）；内容相同 → 幂等跳过（重放语义保留）；retention 已淘汰行（行已删除）重放不报错（淘汰已通过 terminal_retention/gap 呈现）。证据：tests/unit/terminal.test.ts 新增 3 用例（同 seq 不同文本/通道/byte_length/frame_kind → 409、exit payload_json 冲突、淘汰行重放无冲突）+ 既有 reconnect-after-seq/retention-gap/overflow 用例不破坏；根 pnpm test 648/648；run-terminal-tests.sh 11/11（commit 849a788）。剩余：CI job 绑定。 |
| STORE-06 | storage-migrations.md §4 / domain-model §10.1 lease token 哈希存储（审计报告 §4 #4） | 已实现未验收 | migration 0014_lease_token_hash（SCHEMA_VERSION 13，幂等）：jobs 新增 `lease_token_hash TEXT` 并回填（payload.__lease_token 存在的旧行按行 sha256，payload 原样不动）；pty_sessions 重建为 `lease_token TEXT`（可空，旧行保留）+ `lease_token_hash TEXT NOT NULL DEFAULT ''`（与 pty-session.ts 共享 PTY_SESSIONS_TABLE_DDL）。claim 只写 hash 列——明文仅存 kernel 内存（claim 响应返回 runner，传输面明文/存储面哈希；重启后重取 job 记录 lease_token 为 null，fencing 校验 sha256(提供值) vs hash 列重启后仍可验）；heartbeat/complete/terminal frames 的 token 半区改 hash 比较，hash 空旧行走 legacy payload token 比较（fail-closed）；cancel/complete/recover 释放内存 token。PtySession.lease_token wire 形状 nullable。证据：tests/unit/migrations.test.ts 12/12（0014 列/回填/pty 重建 + STORE-08）、tests/unit/kernel.test.ts 112/112（claim 后 hash 匹配、payload 无明文、hash fencing、旧行兼容、token 轮换）、tests/unit/pty-session.test.ts 13/13、根 pnpm test 674/674、run-fencing-tests.sh 15/15、run-terminal-tests.sh 11/11、verify-docs 18/18。剩余：CI job 绑定。 |
| STORAGE-07 | storage-migrations.md §8.2/§10 迁移前 DB backup + CAS inventory + 完整性扫描（审计报告 §4 #2） | 已实现未验收 | bin/kernel.ts 启动钩子 `--backup-on-start` / env `DSH_SCHOLAR_BACKUP_ON_START=1`（默认关闭）：kernel 打开/迁移数据库前对 dataDir/backups/ 生成 `kernel-<ts>.db`（VACUUM INTO 一致快照，0600，即迁移前状态）与 `inventory-<ts>.json`（0600，cas.inventory() 每 blob sha256+size_bytes+mod_time + schema_version_at_backup/database_id/计数）；首启无库跳过注明，失败 loud fail。kernel 新增 `scanIntegrity()`：{missing_blobs, orphan_blobs, size_mismatch, hash_mismatch, scanned/skipped/total}，对 artifact blob 重算 sha256 比对（大 CAS 可 limit 限量），恢复后 12 项扫描的核心 Blob 面落地。证据：tests/unit/backup.test.ts 2/2（快照可打开且迁移前 schema、inventory 与 CAS 一致、0600、缺库 loud fail）+ bin/kernel.ts 冒烟（首启跳过/二次启动生成）、tests/unit/cas-gc.test.ts 6/6（scanIntegrity 篡改/截断/孤儿/缺失/限量/自愈）、根 pnpm test 674/674、verify-docs 18/18。剩余：其余 11 项恢复后扫描（lease/run_id、PTY generation/expiry、Config hash、Intake、Trajectory、Bundle 等）与 CI job 绑定。 |
| STORE-08 | storage-migrations.md §8.1 0003 checksum 绑定共享 DDL（审计报告 §4 #7） | 已实现未验收 | 0003 canonical body 改为「函数源码 + 冻结内联 DDL 快照」（TERMINAL_DDL_0003/TEX_DDL_0003，迁移执行的就是冻结常量，不再引用共享 TERMINAL_DDL/TEX_DDL）：checksum 绑定 0003 实际执行的 DDL——共享常量演进（新能力走新迁移 + tex-workspace/pty store CREATE IF NOT EXISTS 收敛）不破坏既有库已记录 checksum，对 0003 自身文本的任何编辑则 checksum mismatch loud fail（immutability 由 checksum 兜底）。证据：tests/unit/migrations.test.ts STORE-08 用例（body 内联 DDL、无共享常量引用、checksum 为 id+body 纯函数、body 编辑即变）+ 根 pnpm test 674/674、verify-docs 18/18。剩余：CI job 绑定。 |
| FIXT-01 | full-auto 仅允许已登记 FixtureProfile（reconstruction-contracts.md §5 / security-baseline.md §1） | 已实现未验收 | 新增 research-schemas/src/fixture-profile.ts（FixtureProfile schema：code/data/image/expected_outputs + z.literal 强制的 automatic_release=false/allow_private_data=false/allow_external_release=false；注册表含 golden-path-v2）+ ExecutionConfig.fixture_id + registry execution.fixture_id 键；kernel createProject 对 full-auto 强制已登记 fixture_id（422 fixture_required，未注册/缺失一律拒绝、零落库）；submitJob 对 full-auto 项目强制：profile 存在（fixture_required）、data_artifact_ids 按 blob sha256 必须 ∈ profile.data（422 fixture_artifact_outside_profile）、镜像 digest 必须等于 profile.image（422 fixture_image_mismatch，缺省绑定 profile.image）、profile.code.archive_sha256 钉定时 code snapshot 必须匹配（422 fixture_code_mismatch）；plugin research_project tool 新增 fixture_id 参数。证据：tests/unit/full-auto.test.ts 11/11（注册表守卫 rail 不可覆写、create 拒绝/持久化、submit 镜像/数据/防纵深）。剩余：真实 golden-path fixture 端到端（Docker 环境）、CI job 绑定。 |
| ARCH-01 | 归档禁止 running 项目（reconstruction-contracts.md §4 "no running jobs; otherwise 409"） | 已实现未验收 | kernel archiveProject 对 queued/running/retryable 作业 409 jobs_running，项目保持未归档。证据：tests/unit/kernel.test.ts 新增用例。 |
| DOM-01 | 高风险领域停止推进（product-spec.md §1 / README 使用边界） | 已实现未验收 | kernel createProject 对 clinical/human-trial/wet-lab/weapons/biosecurity 等域 422 domain_unsupported（fail-closed，零落库）；默认纯计算域不受影响。证据：tests/unit/kernel.test.ts 新增用例。 |
| HARD-01 | storage-migrations.md §2/§4/§6 小项：busy_timeout、claim 事务、CAS 已有 Blob 复验 | 已实现未验收 | store.ts 增加 PRAGMA busy_timeout=5000（+migrations.test.ts PRAGMA 断言：WAL/FK/busy_timeout）；claimJobs 的 UPDATE jobs + INSERT runs 包 withTransaction（execution-runtime.md §3 “claim 事务同时创建 Run attempt”，tex_builds 状态同步在提交后执行——tex store 独立连接）；cas.ts put() 对已存在 Blob 校验 size（不匹配→blob corruption 拒绝，cas-gc.test.ts 用例）。migrations/README.md 同步为当前迁移实现（migrations.ts 而非 store.ts）。 |
| V1-HEALTH | 实例身份字段双面暴露（test-instance-plan.md §1） | 已实现未验收 | /v1/health 增加 instance_id/protocol_version='v1'/schema_version/database_id（保留 ok/instance/config_pin）；/v2/health 为规范形状（V2-HEALTH）。 |
| UI-AUDIT | gui-plugin-plan.md 纯代码缺口（不依赖浏览器） | 已实现未验收 | Evidence 面板不再硬编码 pill('verified')——按 item.provenance_status 原样渲染（accepted 绿色/verified 蓝色/slate，gui-plugin-plan §10）；命令面板补齐 reproduce 条目（§4 共 17 条）；批量归档 running 过滤由 kernel 层 409 守卫兜底（ARCH-01，sidebar 仍提示）。浏览器级缺口（PTY/Workspace/Preview/Trajectory/Upload/Settings UI 等）如实保留（🌐）。 |
| UI-NAV2 | gui-plugin-plan.md §5.1 Overview 结构化 NextAction v2 卡渲染（审计报告 §4 #11） | 已实现未验收 | client 逻辑层闭环（commit 849a788）：纯模型 packages/dsh-research-ui/src/client/next-action-cards.ts——nextActionCardModel(action, locale) → {code, title, tone, disabled, reasonText, missingList, route, hasRoute, blockingNote, stateLabel, isUnknown}（三态 tone、done 灰显/blocked 有缺口禁用/ready 高亮、required 缺口已知 code 翻译未知原样、route 映射 gates/runs/evidence/manuscript/budget 直达 + ideas/contracts/release/overview 收敛 Overview tab + 未知 route 安全回退、code='unknown' 只读无 CTA 不构造 mutation、缺失字段安全退化）+ resolveNextActionInput（v2 优先，缺失/畸形 v2 回退 legacy `next_actions: string[]`，向后兼容）；DOM 装配 panels/overview.ts（code 徽标、ready/blocked/done 三态类名 + CSS、reason 行、blocked 点击展开缺失前置列表、route 按钮经 state.activeTab+tabSave+rerender 与 nav.ts `#tab=` 深链跳转、blocking 说明），phase.ts 接入；i18n overview 命名空间新增 nextaction.* 键（state 文案/CTA/blocking 说明/19 个 code 标签/6 个缺口映射，zh/en parity；gate_decide/job_retry 标签含动态数据不登记，原样显示内核 label）。证据：tests/unit/next-action-cards.test.ts 26/26（三态/禁用/missing/route/unknown 退化/legacy 回退/字典键存在 + 零缺 key）、i18n-chrome.test.ts 3/3、根 pnpm test 627/627、research-ui typecheck+build 全绿、node scripts/verify-docs.mjs 18/18。剩余（如实记录）：卡片 DOM 布局/动画/键盘与 a11y、640/720/1024 视口下的卡渲染需 Playwright 类浏览器环境，未验收。 |
| DOC-SYNC | README/USAGE_GUIDE/§15/§18/聚合器注释/账本与代码同步 | 已实现未验收 | USAGE_GUIDE §2/§6/§6.1/§6.2/§9/§10/§11 过时描述更新（PTY/Workspace/RUN-REMOTE/TRAJ/CONFIG/REL-01/ONBOARD 现状）；README 开发状态列表改为“服务端/契约层已实现 + 浏览器层剩余”；run-all-v2-blocking-tests.sh 头注释写明两个有意不入聚合器的脚本（run-latex-tests.sh 走 CI latex job、run-standalone-http-tests.sh 嵌套于 run-hardening-tests.sh）；docs/README.md §4.1 修正 verify-docs --diff-check 已 fail-closed 的描述；reconstruction-contracts.md §15/§18 补现状注记；migrations/README.md 修正（§见 HARD-01）。证据：node scripts/verify-docs.mjs 18/18。 |
| PROF-01 | runner_profile_id opaque profile 注册表 + Job 固定 profile/config hash（domain-model.md §2/§9.1，审计 §4 #8） | 已实现未验收 | research-schemas `runner-profile.ts`：RunnerProfile 记录（opaque profile_id/display_name/runner_mode(local-docker|isolated-subprocess)/锁内 image digest/network_policy=none/limits/capabilities/config_hash+enabled，`.strict()` 拒绝 docker flags/hostname/credential/endpoint）+ 内置注册表（profile_local_docker_cpu_v1、profile_local_docker_gpu_v1（无 GPU 路径，CPU-only pin）、profile_isolated_subprocess_v1（trusted-smoke-fixture 专用）），image 与 images.lock.json node_fixture digest 对齐（读锁文件，缺失回退同源常量）+ `computeProfileConfigHash`（canonical JSON sha256 pin，稳定/变更敏感）+ legacy enum→id 映射 + `resolveRunnerProfile`（未知 id 抛 RunnerProfileUnknownError）。ExecutionConfig 增可选 `execution.runner_profile_id`（config-registry 新键 `execution.runner_profile_id`，configs/generated 重新生成；优先于 v1 enum；未知 id 在 createProject 与 submitJob 均 422 `runner_profile_unknown` 零落库）。kernel submitJob 对 secure kinds 注入 `payload.runner_profile_id` + `payload.profile_config_hash`（JobSpecBinding 增两字段，DB read-back 保留；job 级 > project 级 > enum）；isolated-subprocess 经 profile 解析后 secure kinds 仍 422 `container_execution_required`（trusted-smoke-fixture 保留）。runner executeJob 按注册表复算校验（未知 id/hash 不一致 → completeJob failed/`failure_class=environment` 绝不执行），docker 参数（limits/network/opaque profile_id）取自 profile 记录，缺省值与既有容器基线字节级一致（buildLocalDockerArgs 不变）；ExecutionPlan 增 `profile_config_hash` pin（plan 固定 profile/config hash，`.strict()` 保持）；RemoteFleetServer buildPlan 同一 profile 解析（不一致 → 409 profile_config_hash_mismatch retryable，不带病分发）。证据：tests/unit/runner-profile.test.ts 11/11（注册表解析/opaque 语义 strict/锁对齐/GPU CPU-only 注记/isolated 限制、config_hash 稳定+变更敏感+自洽）、tests/unit/kernel.test.ts 109/109（submitJob 注入+read-back、优先级覆盖、未知 id 422 双面、isolated 422、latex pin）、根 pnpm test 664/664、research-schemas/research-kernel/runner-gateway build 全绿、run-terminal/run-fencing 不破坏、verify-docs 18/18。剩余：GPU 真实执行路径（环境无 GPU，如实保留）、CI job 绑定。 |
| SHAPE-01 | v2 形状对齐组（domain-model.md §5/§6/§8/§9/§16，审计报告 §4 #9） | 已实现未验收 | 六项协调改动（schema + kernel 写路径，全部向后兼容）：① `CorpusSnapshot.schema_version`（默认 1）+ `source_status`（pending/complete，默认 complete）由 snapshotCorpus 写入；② `Passage.content_hash` 新写必填（kernel 快照写入强制 sha256(text)，验证非空，客户端不可声明 hash）旧读兼容（无字段的旧 passage 仍解析，is_untrusted 语义不变）；③ `IdeaCard.corpus_snapshot_id`（可空默认 null）+ Idea Gate 决策绑定校验（card 携带时 snapshot 必须存在且同项目，否则 422 `idea_corpus_unknown`/`idea_corpus_foreign`；旧卡不拦截）；④ `ArtifactKind` 扩展 tex-source/bib/compile-log/compile-aux（kernel registerArtifact + server ARTIFACT_KINDS 同步放行；runner latex-compile 完成路径以 compile-log/compile-aux 注册 TeX 日志与 aux 打包，PDF 保持 pdf）；⑤ `jobs.created_by_principal_id`（migration 0016 可空列；submitJob 接受可选 principal 落库；HTTP 作业路由从 BFF x-principal-id 解析——standalone BFF 在 /v2/* 全量与 v1 jobs 路由注入 loopback operator 身份，body 覆写仅供内部调用方，缺省 NULL）；⑥ `BudgetRecord.storage_bytes`（默认 0；migration 0016 列；recordUsage 事务内原子累计）。证据：tests/unit/v2-shape.test.ts 15/15（六项正/负向 + 旧数据兼容 + HTTP 面）、migrations.test.ts 15/15（0016 列/SCHEMA_VERSION 15/v1 fixture 导入不破坏）、根 pnpm test 692/692、research-schemas/research-kernel build 全绿、run-standalone-http-tests.sh 180/180（BFF v1 jobs principal 注入不破坏）、run-gate-tests.sh 15/15 + run-evidence-tests.sh + evals/latex-compile-e2e.sh 不破坏、verify-docs 18/18。剩余：CI job 绑定。 |
| MIG-V1 | storage-migrations.md §9 v1 迁移补 3 步：synthetic_fixture、unsigned_legacy、旧 log→final log Artifact（审计报告 §4 #6） | 已实现未验收 | migration 0017_v1_legacy_marks（SCHEMA_VERSION 15，独立追加迁移——不改 0002 函数体、不动 0001–0016 已发布 checksum）：① jobs 新增 `synthetic_fixture INTEGER NOT NULL DEFAULT 0`，v1 旧行按 `kind IN ('echo','smoke')` 回填 1（message-only fixture 运行），新 echo/smoke 作业由 submitJob 写入 1、非 fixture 新行恒 0；② jobs 新增 `signature_status TEXT`（仅旧行；新行 NULL 由 runs.signature_status 治理）——run_manifest 无签名的旧作业与 succeeded 且无 manifest 的非 fixture 旧作业回填 `legacy_unsigned`，runs 旧 `unsigned` 行与 `pending`+无签名 manifest 不一致旧行同回填（"必签 Manifest"默认开启后旧数据不被误判为新违规）；③ jobs 新增 `legacy_log_artifact TEXT`——v1 payload 内联 stdout/stderr 迁移时经 openDatabase/runMigrations 可选 casRoot 写入 CAS 并建 `kind='log'` Artifact 行（metadata legacy_log:true、file_name legacy-run-<job_id>.log），引用记在 job 上，**不伪造 terminal frames**；无 casRoot 时仅记 `legacy:in-payload` 标记（绝不创建引用缺失 Blob 的 Artifact 行）。build-v1-fixture.mjs 新增 echo/smoke、无签名 manifest、payload 日志字段并重建 v1-kernel.db。证据：tests/unit/migrations.test.ts 15/15（fixture 全链标记+Artifact+CAS 一致、rewind 幂等、新行不受回填影响、无 CAS 标记路径、SCHEMA_VERSION 15）、根 pnpm test 全绿、research-kernel build 全绿、verify-docs 18/18。剩余：CI job 绑定；commit d960f34。 |
| TEX-SAVE | storage-migrations.md §5/§7 / domain-model.md §12：TeX save 单事务 + tex.file.saved Outbox 事件（审计报告 §4 #3） | 已实现未验收 | tex-workspace.ts writeFile/deleteFile/moveFile 包单事务（withTx：BEGIN IMMEDIATE/COMMIT/ROLLBACK；`isTransaction` 守卫使 moveFile→writeFile 嵌套复用同一事务，无嵌套 BEGIN）——最后一条语句（revision bump）注入失败整体回滚：writeFile 无文件行且 revision 不变、deleteFile 文件原样保留且 revision 不前进、moveFile 源保留且目标不出现；成功路径文件行与 revision 原子落库。kernel texWriteFile 成功后追加 `tex.file.saved` Outbox 事件（KernelEventKind 新增；payload: project_id/document_id/path/revision + 可选 request_id/session_id；aggregate_type='project'/aggregate_id=project_id/aggregate_revision=保存后 revision；event_seq 按 project aggregate 单调）；409 document_version_conflict 不发事件；HTTP PUT file 路由以 x-request-id/x-principal-session 关联事件；tex.file.saved 投影为 research lane（trajectory.ts RESEARCH_KINDS + redacted summary）。跨连接原子性取舍：tex store 是独立 WAL 连接，tex 写先提交、outbox 后写，outbox 追加失败记录 error 不阻塞保存（写已提交、客户端已见成功）。证据：tests/unit/tex-workspace.test.ts 12/12（新增单事务 4 用例）、tests/unit/tex-event.test.ts 6/6（新增文件）、tex-build/tex-kernel/tex-preview 不破坏、根 pnpm test 702/702、research-schemas/research-kernel build 全绿、evals/latex-compile-e2e.sh 13/13、verify-docs 18/18。剩余：CI job 绑定。 |
| FLEET-01 | 生产 runner 二进制接线 fleet 服务（remote-runner-wire.md §2/§8 → §9 生产接线，审计报告 §4 #10） | 已实现未验收 | runner 二进制新增两个互斥角色（fleet-cli.ts + bin/runner.ts，Config Registry runner-profile scope 新增 5 键：runner.fleet_server_port/fleet_url/agent_id/fleet_target_id/fleet_public_key，configs/generated 已重新生成）：`--fleet-server <port>` 启动 RemoteFleetServer（attachRemoteFleetRoutes 挂真实 HTTP 路由、x-service-token 鉴权、注册表复用 InMemoryAgentRegistry、kernel client 复用本地 runner 同一 ResearchClient 路径、plan 签名密钥 = --key-file、公钥打印供 agent 验签、`--fleet-server 0` 临时端口）；`--agent <fleet-url>` 启动 RemoteRunnerAgentImpl 客户端循环（register 带重试 → runPollLoop：heartbeat → flush spool → claim → 执行 → frames/artifacts/complete；离线有界 spool 复用 AgentOutboundSpool；--fleet-public-key 验签缺省 fail closed；--key-file 签名 manifest；显式 --kernel 时尽力注册 manifest 公钥到 kernel，非致命）。互斥校验 fail fast：--fleet-server 与 --agent 同给、任一 fleet 角色与 --mode 同给均拒绝（--mode 仅本地模式有意义）；本地 --kernel 模式行为完全不变。**接线时修复**：kernel-client.ts appendTerminalFramesWithLease 未绑定调用 client.request（this=undefined → timeoutMs TypeError）——本地 runner 的 .catch() 掩盖为帧静默丢失、fleet 转发 frames 报 502 kernel_unreachable；改 request.call(client,…) 绑定，真实 kernel e2e 全链通过。证据：tests/unit/fleet-bin.test.ts 11/11（真实 node:http listener + HttpRemoteFleetTransport：角色判定与互斥、registry 键解析/校验、固定端口绑定 + 403、全链 runFleetAgentMain 完成 Job、离线 spool HTTP 层恢复、无匹配 target 留 pending + 匹配 agent 接单、未注册 404、appendTerminalFramesWithLease 绑定回归）、remote-wire.test.ts 15/15 不破坏、根 pnpm test 全绿、research-schemas/runner-gateway build 全绿、verify-docs 18/18、真实 kernel+fleet server+agent 三进程 e2e（echo job queued→succeeded，agent 公钥注册成功）。剩余（如实记录）：真实 mTLS 证书链（CA/第二主机）验收、跨主机网络分区故障注入、真实远端 sandbox 隔离验收（🌐，commit 9ff6d61）。 |
| 审计结论 | 审计轮剩余两大项 OBS-01（/internal/metrics）与 STORE-05（terminal_frames 完整性）已于后续实现轮关闭（见上两行）；审计报告 §4 #10 的 runner 二进制 fleet 接线由 FLEET-01 行关闭（见上两行），无"可实现但未实现"差距 | — | 聚合器 SCRIPTS 20 项与实际脚本一致（两项有意排除已注释）；acceptance-tests.md 场景抽样 40+ 条均有对应测试；CLI 参数（kernel/runner/orchestrator/standalone）与 parseCli 一致（orchestrator 本轮补齐；runner 本轮新增 fleet 角色）；关键路径无遗留 TODO/FIXME/501（PTY 无 adapter 501、RemoteRunnerAgentNotImplementedError 均为设计）。环境受限清单（真实浏览器 Playwright、真实 mTLS/远端主机、真实 Docker/TeX/GPU/AV 深度扫描、DSH host 全链验收）见审计报告。 |
| 收尾状态 | 代码侧全部实现（2026-08） | — | 审计报告已归档至 docs/final-audit-report.md。全部 §4 P0/P1 服务端/逻辑层项、审计 §4 的 11 项剩余大项（OBS-01/STORAGE-07/TEX-SAVE/STORE-06/STORE-05/MIG-V1/STORE-08 冻结规则/PROF-01/SHAPE-01/FLEET-01/UI-NAV2）均已实现并验收（单元测试 713/713、聚合器 19/19、ci-gate 4/4 零 SKIP、golden-path-v2 29/29 真实 Docker 全链）。剩余项全部为环境受限验收（浏览器 Playwright、真实 DSH host、真实 mTLS/远端主机、GPU、AV 深度扫描、双进程 orchestrator 并发、真实 registry 发布），代码侧无可实现差距；产品保持 Security Alpha/"已实现未验收"，不越级宣称。8443/18610 持续在线。 |

## 5. 2026-08-11 当前代码审阅阻断项

审阅范围：`cf6a8a8...fda346b`，`main` 与 `origin/main` 一致。本节覆盖 §3、§4 和历史“收尾状态”的完成声明；同一能力出现冲突时一律采用本节较低状态。当前结论是：**产品仍为 Security Alpha，不可发布，代码侧仍有可实现的 P0/P1 差距**。

当前提交的可重复验证结果：

- `node scripts/verify-docs.mjs`：18/18 通过，但该检查只验证结构、片段与静态删除面（本轮注册 19 个必查文档后为 19/19）；
- `./node_modules/.bin/vitest run`：43 个测试文件通过、2 个 suite 失败；703 个测试通过、1 个失败、9 个因 `pnpm` 不在 PATH 而跳过；失败项为 workspace 子目录权限在 `umask 0077` 下实际 0700、契约要求 0750；packaging suite 因 `spawnSync pnpm ENOENT` 未执行；**两项已在本轮修复并重跑全绿（见下表 WORK-01/CI-01 行：workspace-store.test.ts 16/16、packaging.test.ts 10/10、security.test.ts 26/26）**；
- `bash tests/security/run-selfmod-tests.sh`：17/18；生产隔离、显式 dev overlay 和真实 DSH dump-config composition 通过，tarball 负向断言因 `pnpm pack` 未产出 tarball 失败（脚本直接调用裸 `pnpm`，环境 PATH 无 pnpm）；因此 SELFMOD-01 只能记“已实现未验收”；**已修复（resolve_pnpm 解析 pnpm 入口）并本轮重跑 19/19**；
- 文档中的 713/713、19/19、4/4、29/29 是历史证据，不能替代上述 `main@fda346b` 当前结果。

| 优先级 | 影响 ID / 当前状态 | 当前阻断 | 强制关闭条件 |
|---|---|---|---|
| P0 | API-01、SIDE-01：已实现未验收 | standalone sidecar 只向 Kernel 注入 `DSH_SCHOLAR_SERVICE_TOKEN`，未设置 `DSH_SCHOLAR_KERNEL_TOKEN`；Kernel 在普通 token 未配置时跳过整个 Bearer 检查。任何能访问 loopback Kernel 端口的本地进程可直接调用普通 v1/v2 API。 | sidecar 每次启动生成独立普通 Kernel token，通过 0600 文件/env 传递；BFF/Runner/Orchestrator 全部带 token；除 health 外缺失/错误 token 一律 401；直接访问 sidecar 端口的读写负向测试必须通过。**§5 行 1 P0 阻断项已关闭（commit 4b92ed8）**：plugin 与 standalone 两个 sidecar 均按 service-token 同款模式维护 0600 `<dataDir>/kernel-token`（首次随机 32 hex、之后复用；plugin 的显式 `token` 配置仅作首次种子，文件权威），spawn kernel 恒以 env 注入 `DSH_SCHOLAR_KERNEL_TOKEN`（永不进 argv）；kernel server 在配置 token 后对除 `/v1|v2/health` 外的全部路由强制 `Authorization: Bearer`（缺失/错误一律 401 `unauthorized`），无 token 的裸 kernel 才跳过检查（显式开发模式，仅单元测试裸启动）；BFF 全部上游请求（proxy + 内部 lookup）带同一 bearer，plugin client 用 `sidecar.kernelToken`，runner 二进制 `--token` 缺省回退 `DSH_SCHOLAR_KERNEL_TOKEN` env，orchestrator `--token-file` 之外新增同 env 回退；两层凭据严格分离——bearer 不解锁内部路由（仍 403 `service_token_required`），x-service-token 也不能替代普通 bearer（401）。证据：tests/unit/kernel.test.ts server 矩阵（无/错/对 bearer 401/401/200、health 豁免、metrics 仍在 bearer 后、内部路由双层互不替代、裸 kernel 跳过、ResearchClient 正/负向）、tests/unit/sidecar.test.ts（两个 sidecar 的 kernel-token 文件 0600/32hex/复用/跨实例一致、stop 不删文件、显式种子、spawn 后直接端口读/写无 bearer 401、文件 token 200、health 豁免、service-token 独立文件）、run-standalone-http-tests.sh 新增直接 sidecar 端口负向段（读/写/错 token 401、文件 token 200、service token 不可替代、health 豁免、内部路由 bearer-only 403 + 双凭据 200）、run-sse-tests.sh 直接 BFF kernel 调用带 kernel-token 适配。剩余：CI job 绑定；浏览器层经 BFF 面行为不变。 |
| P0 | API-01、PTY-01：已实现未验收 | BFF 只为 project/job/child（以及 Gate Decision）解析项目。Artifact、Document/TeX、PTY session、全局 events 等 global-id 路由在 `memberProjectId=null` 时直接转发；PTY GET/frames 无 owner 检查，control 仅在可选 header 存在时检查，BFF 又只在 open 时注入该 header。知道 ID 即可跨项目读取或控制。 | ✅ **已关闭（2026-08-11 修复轮）**。**BFF 统一 global-id resource→project resolver**（packages/dsh-research-ui/src/standalone/server.ts，每类一个 authoritative 解析函数、走 kernel 查询、解析失败/未知 id/非成员一律 404 防枚举）：`artifactProjectId`（GET/HEAD `/v1/artifacts/{id}`——HEAD kernel 路由读权威 `x-project-id` 响应头，`?project_id=` 存在时直接校验）、`documentProjectId`（`/v1/documents/{id}/*` 全部子路由——GET …/tree 的 `document.project_id`）、`ptySessionProjectId`（`/v1/pty/sessions/{id}*`——带注入 principal 读 kernel 会话行，kernel 对非 owner 的 403 在 BFF 同为 404，成员也探测不到他人会话项目）、`eventsProjectId`（`/v1/events` 必须显式 `?project_id=`，无 scope 的跨项目 dump 直接 404 fail-closed）；`globalResourceProject` 派发 + `isGlobalIdRoute` 兜底（无法解析的 global-id 路由 404，kernel 的 422/409 形状不穿透 BFF）。解析后先 membership（非成员 404，与 project-scoped 同契约）再转发，role 策略自动生效（viewer/auditor 禁写 document/artifact/pty control）。PTY 身份头注入从仅 open 扩展到全部 `/v1/pty/sessions/*` 转发（server-derived，客户端值永不信任），`x-pty-lease` 透传（BFF 不读不铸，kernel 校验）；`POST /v1/artifacts` 按 body project 补 membership/role 检查（镜像 pty-open 规则，跨项目注册 404）。**membership 实时查询**：删除 BFF 永久 Promise 缓存，撤权后下一次请求立即 404（acceptance §21 membership-revocation-no-stale-cache）。**Kernel PTY 强制 principal+owner+lease**（packages/research-kernel/src/server.ts `requirePtyOwner` + pty-session.ts `verifyLease`：sha256+timingSafeEqual 常数时间，STORE-06 hash 列为主、legacy 明文列回退、两者皆无 fail-closed）：GET `sessions/{id}`、control、frames 全部缺 `x-principal-id` → 422 principal_required（GOV-01 模式）、非 owner → 403 pty_principal_mismatch（与既有 control 检查一致）、control 缺 `x-pty-lease` → 403 lease_required / 错 → 403 lease_invalid、frames 可选 lease 但给错仍 403——“header 缺失即放行”不再存在；未知会话仍 404 pty_session_not_found。证据：tests/unit/pty-session.test.ts 14/14（新增 direct-kernel 负向矩阵：无 principal 422、owner 不符 403、lease 缺失/错误 403、frames 错 lease 403、跨项目 control 403、未知 id 404 + 正向 owner+lease 200）、tests/unit/kernel.test.ts 116/116、根 pnpm test 全绿；tests/security/run-hardening-tests.sh 新增 direct-kernel PTY 段（say/ok/bad 同矩阵 + 正向 control 200）；tests/security/run-standalone-http-tests.sh 新增 P0-2 段（跨项目 artifact/document/pty/events 404、成员 200、猜 ID 404、无 scope events 404、body 注册进 foreign 项目 404、撤权后同一 BFF 下一次请求立即 404——原 180 断言全部保留）；run-sse-tests.sh 不破坏；verify-docs 19/19。剩余（人工，NOT_RUN_MANUAL_PENDING）：浏览器级 interactive-terminal-browser 与多人真实撤权验收（manual-acceptance.md §4 队列）。 |
| P0 | TEX-01、TEX-03：已实现未验收 | **代码侧闭环（2026-08-11 修复轮）**：① “打开/ensure 文稿”只读或首次创建——新增 `GET /v1/projects/{id}/manuscript-drafts`（只读返回现有 workspace `{document_id, revision, files, created}`；无内容时 404 `manuscript_not_found`，绝不建行/写字节）；`POST manuscript-drafts` 默认 ensure 语义（文档已有文件即原样返回 created=false，不重写 `paper.tex`/`main.bib`、revision 不变——保存 A → rerender → GET 内容仍为 A、revision 未变）；只有无 document/无文件（首次）才生成。② 显式 regenerate——`regenerate=true` 才重写，生成前把当前内容冻结为 revision-scoped snapshot（旧字节经 `GET snapshot-files?revision=&path=` 可回退，生成即新 revision，多次 regenerate 的每个旧版本都可回退）；UI 提供 Regenerate 按钮带 window.confirm 确认。③ client 面板渲染改为“先 GET（只读），不存在才 POST 创建”（`resolveOpenDocument` 纯逻辑），rerender/轮询/locale 切换/保存后重绘零写入。④ 保存成功（PUT file 200）后调用一次 `POST /v1/documents/{id}/preview-builds`（debounce 由服务端 800ms 承担、快速连续保存合并；preview-builds 服务端 1907897 未动，仅接线 client），UI 轮询 `GET preview-builds` 投影并展示 pending/queued/running/succeeded/failed/cancelled/superseded/stale 状态文本 + 最新 succeeded preview 的 PDF embed/下载。证据：tests/unit/tex-ensure.test.ts 8/8（首次创建、二次 ensure 不重写、保存 A→ensure 内容/revision 不变、regenerate 新 revision+旧版可回退、双次 regenerate 双版本可回退、裸文档首生成、HTTP 404/ensure/regenerate 全链）、tests/unit/manuscript-flow.test.ts 15/15（先 GET 后创建、preview 面板模型、保存后触发 preview hook 一次且失败不阻塞保存）、tex-kernel.test.ts 9/9、tex-preview.test.ts 12/12、tex-workspace.test.ts 12/12、tex-build.test.ts 8/8、根 pnpm test 全绿、research-ui build+typecheck 全绿、latex-compile-e2e 13/13 不破坏、verify-docs 19/19。**剩余（浏览器视觉验收，Playwright 类环境不可用，如实保留，记 NOT_RUN_MANUAL_PENDING）**：保存→debounce→PDF 自动刷新的视觉链、preview 状态文本/PDF 同页观感、stale 标识视觉、regenerate 确认对话框交互——队列见 manual-acceptance.md §4（manuscript-open-never-regenerates / tex-save-live-preview）。 | “打开/ensure 文稿”只读或首次创建；显式 regenerate 需确认并保留版本；保存+rerender 不改变字节/revision；保存成功触发 debounce preview，UI 展示 pending/running/failed/stale/PDF；数据不丢失与实时预览**浏览器**验收（NOT_RUN_MANUAL_PENDING）。 |
| P0 | SNAPSHOT-01、API-01：已实现未验收 | ~~`POST /projects/{id}/code-snapshots` 接受调用者提供的 host `path`，`snapshotCodeArchive` 对 `resolve(path)` 后的任意目录递归读取并写入 CAS；只做根内 symlink 检查，没有把根限制到已批准 workspace。~~ **代码侧闭环（2026-08-11 修复轮）**：API 只接受 `workspace_id` + root-relative `root_relative_path`（缺省 `''` = 整个 workspace）；服务端从批准的项目 workspace 解析实际根（`dataDir/workspaces/{project_id}/{workspace_id}/`，kernel `snapshotCodeArchive(projectId, workspaceId, relativePath)`，跨项目/未知 workspace → 404 形状 `workspace_not_found`，不可枚举）；相对路径复用 `normalizeWorkspacePath`（绝对路径、`..`/`.` 段、NUL、盘符、反斜杠、空段全部拒绝，尾斜杠裁剪接受），并加 realpath 容器校验——workspace 目录或子根被 symlink 替换指向 workspaces 区外 → 422 `snapshot_path_escape`；walk 保留并扩展 symlink 检查（任何逃逸出根的 symlink → 422 `snapshot_path_escape`，根内 symlink 仍跟随）；secret 文件（`.env`/`.env.*`、`*_token`/`*-token`/`*.secret`/`*.password`/`*.credential*` 类、`*.key`/`*.pem`、`id_rsa`/`id_ecdsa`/`id_ed25519`/`id_dsa`、`.aws/credentials`、`service-token`/`kernel-token`、`.npmrc`/`.pypirc`/`.netrc`/`.htpasswd`）**拒绝整个快照**并列出全部文件名（422 `snapshot_secret_file`，CAS/Artifact/manifest 零写——fail closed，不做 skip-and-record）；SNAPSHOT_MAX_FILES/MAX_FILE_BYTES/MAX_TOTAL_BYTES 上限保留；`code_snapshots.source_json` 记录 workspace_id + root_relative_path 溯源（archive/manifest/registry 仍只用 `~` 显示根，宿主路径零泄漏）；旧 `{path}` 形状由 strict schema 直接 422（api-contracts.md 标注废弃）。证据：tests/unit/snapshot-root.test.ts 8/8（workspace 根 + 相对子目录成功、跨项目/未知 404、绝对/`..`/盘符/NUL/空段 422、尾斜杠接受、根内逃逸 symlink 与 workspace 根 symlink 替换 422、secret 全清单 + CAS/Artifact 零写、上限保留、字节一致）、tests/unit/kernel.test.ts 既有快照用例（归档内容、unpack、上限、host-path 泄漏、registry runs）全部迁移到 workspace 语义后通过、tests/security/run-hardening-tests.sh 新增 P0-4 HTTP 段（workspace 根快照 201 + sha256、相对子目录 201、旧 `{path}` 形状 422、绝对/`..`/盘符 422 invalid_path、跨项目 404、secret 422 且列出 `.env`）。剩余：真实 golden-path Docker 端到端与浏览器/人工验收（队列见 manual-acceptance.md §4，记 NOT_RUN_MANUAL_PENDING）。**§5 行 4 P0 阻断项已关闭（commit 4b92ed8）**。 |
| P1 | RUN-REMOTE-01、RUN-01a：已实现未验收 | **代码侧闭环（2026-08-11 修复轮）**：远端 Agent 默认执行器按 kind 路由——secure kinds（baseline/pilot/formal/reproduce/latex-compile）只能进入 digest-pinned restricted container（与本地 docker 路径逐项一致的参数构造 buildLocalDockerArgs：`--network none`/`--user 65534:65534`/`--read-only`/`--cap-drop ALL`/`--security-opt no-new-privileges`/pids/memory/cpus 取自 plan.limits，image = plan 固定的 `docker:<digest>`）；docker 不可用 → 该 kind 明确失败 `failure_class=environment`，绝不 subprocess 执行；subprocess 仅保留 echo 与显式 trusted smoke fixture（smoke 未标记 `trusted_fixture` 也拒绝——ExecutionOutputContract 新增 `trusted_fixture`，job payload 固定进 plan）。证据：tests/unit/remote-wire.test.ts（secure kind 无 docker → environment 失败 complete 且无宿主执行；docker 可用 → 容器参数断言 digest/完整基线 + 成功；defaultSubprocessExecutor 对 secure kind/未标记 smoke → environment 失败、trusted smoke → 执行成功）、tests/unit/execution-target.test.ts（plan 字段）、根 pnpm test 802/802 全绿。剩余（真实 mTLS/两主机，NOT_RUN_MANUAL_PENDING）：远端 wire 强制 mTLS/不可伪造 agent identity、证书轮换/吊销与断线重连、真实两主机故障注入验收（队列见 manual-acceptance.md §4）。 | secure kinds 只能进入 digest-pinned restricted container（已实现）；subprocess 仅保留显式 trusted fixture（已实现）；远端 wire 强制 mTLS/不可伪造 agent identity、证书轮换/吊销与断线重连（真实 mTLS 未接线，NOT_RUN_MANUAL_PENDING）；真实两主机故障注入验收（NOT_RUN_MANUAL_PENDING）。 |
| P1 | RUN-REMOTE-01、RUN-01b、ART-01：已实现未验收 | **代码侧闭环（2026-08-11 修复轮）**：① remote 与 local 复用唯一 manifest builder（workers/runner-gateway/src/run-manifest.ts）——remote complete 的 manifest 现携带 `container_digest=docker:<plan digest>`、`data_hash`、`code_commit`、`code_snapshot_id`、`seed`、`metrics_artifact`（容器写回的 MetricsFileV1，与本地 runner 同一校验 metrics-file.ts）与 `run_id`；② kernel completeJob 对 secure kinds：required facts（metrics_artifact 存在/seed 与 job 固定 seed 匹配/code_snapshot_id 与 job 绑定一致/container_digest 与 digest-pinned image 一致/data_hash 与 job 声明一致）缺失或不一致 → 422（manifest_facts_missing/manifest_seed_mismatch/manifest_snapshot_mismatch/manifest_container_mismatch/manifest_data_mismatch），manifest.run_id 必须等于本 attempt 的 runs.run_id（缺失/旧 attempt → 422 manifest_run_mismatch）；③ Fleet 层：complete/stage/finalize 顶层 run_id 必须与 claim 的 plan.run_id 一致（旧 attempt → 422 run_id_mismatch）；④ Fleet CAS 改 **bytes streaming**（kernel artifact 原生字节 → base64，不再 UTF-8 text 编解码），fleet client 请求 kernel 全程带 Authorization Bearer（`--token`/`DSH_SCHOLAR_KERNEL_TOKEN` 传入；401 fail fast 非 retryable，不静默当 cas_missing）；CAS 拉取带 project binding（caller 声明 project_id 不能越过 claim 所属项目 → 403 cas_project_forbidden）。证据：tests/unit/remote-wire.test.ts（manifest facts 与 local 同源断言、complete run_id 不匹配 422、缺 facts 422、二进制 CAS round-trip NUL/0xFF 逐字节一致、project binding 403）、tests/unit/fleet-bin.test.ts（真实 HTTP kernel stub：无 token 401 fail fast + 带 token 字节往返）、tests/unit/kernel.test.ts（manifest_run_mismatch/manifest_facts_missing/manifest_seed_mismatch/manifest_container_mismatch 负向 + analysis/§12.5/STAT-01/duplicate-seed 全链 secureManifest 适配）、根 pnpm test 802/802 全绿。剩余：真实两主机 binary CAS 验收（NOT_RUN_MANUAL_PENDING，队列见 manual-acceptance.md §4）。 | remote 与 local 复用唯一 manifest builder（已实现）；required facts 缺失即 422（已实现）；job/run/lease/manifest run_id 全链一致（已实现）；Artifact 全程 bytes streaming + Bearer + project binding（已实现）；二进制 round-trip、stale attempt 和字段缺失负向测试（已实现）。真实两主机验收（NOT_RUN_MANUAL_PENDING）。 |
| P1 | GOV-01、ONBOARD-01：已实现未验收 | **代码侧闭环（2026-08-11 修复轮）**：① **BFF capability route table**（packages/dsh-research-ui/src/standalone/server.ts `PI_ONLY_WRITE_ROUTES`/`isPiOnlyWrite`）——governanceWrite 子串正则改为显式 route table：transitions/gates/decisions/budget/approve/accept（既有）+ intake `…/intake/{iid}/adopt`（POST /v1/projects/{id}/intake/{iid}/adopt）+ project `archive`/`unarchive`；researcher 对这些写一律 403 `role forbidden`（viewer/auditor 全写 403 既有），BFF 永不转发。② **Kernel 双层校验**（packages/research-kernel/src/server.ts `requirePiOnly`，与 BFF 共用同一份 route table）：adopt/archive/unarchive 路由自身校验 principal 的 PI/operator 身份——有 x-principal-id 时从 kernel 自己的 `project_members` 表解析角色（researcher/viewer/auditor → 403 role_forbidden、非成员 → 404 project_not_found 防枚举、缺身份 → 422 principal_required，GOV-01 fail-closed 模式），不依赖 BFF 单层；BFF 对这些转发注入 server-derived x-principal-id/x-principal-role（客户端值永不信任）。③ **membership 实时查询**：BFF `projectMembers` 每次请求实时查 kernel `project_members`（无 Promise/result 缓存），撤权后下一次请求立即 404（acceptance §21 membership-revocation-no-stale-cache）。证据：tests/unit/kernel.test.ts 117/117（新增 v1 PI-only 矩阵：researcher/viewer 403、非成员 404、无 principal 422、pi archive/unarchive 200）、tests/unit/intake.test.ts 32/32、tests/security/run-standalone-http-tests.sh §5 P1 段（BFF researcher/viewer/auditor adopt/archive/unarchive 403 + 稳定 role-forbidden body、kernel direct researcher 403 / 无 principal 422 / 非成员 404、PI 全链 intake adopt 200 + archive/unarchive 200、撤权后同 BFF 下一请求 404）+ 原有断言不破坏（全套 246/246）、run-hardening/run-gate 不破坏、根 pnpm test 754/754 全绿、verify-docs 19/19。剩余：CI job 绑定；浏览器角色 UX（researcher 视图不出现 adopt/archive 操作入口）与多人真实撤权观感人工验收（NOT_RUN_MANUAL_PENDING，队列见 manual-acceptance.md §4）。 | capability policy 使用显式 route table（已完成——BFF 与 Kernel 同一份表，双侧不漂移）；Kernel/BFF 双层校验 PI-only（已完成——kernel 从自身 membership 表解析角色）；membership 实时查询、撤权立即生效（已完成——无缓存，acceptance §21 membership-revocation-no-stale-cache 已同步措辞：researcher/viewer/auditor 对 PI-only 403，pi/operator 放行）；researcher adopt/archive 与 revocation 负向测试（已完成）。剩余：CI job 绑定与浏览器角色 UX/多人撤权人工验收。 |
| P1 | ONBOARD-01、UPLOAD-01、GUIDE-01：已实现未验收（2026-08-11 修复轮；浏览器视觉验收 NOT_RUN_MANUAL_PENDING） | **代码侧已闭环（2026-08-11）**：(1) **显式 Init/Resume/导入选择**——Start 屏改为「未选中项目即显示三入口」（index.ts `target = state.projectId`，删除 `projects[0]` 自动回退；nav.ts 纯函数 `startScreenVisible`（未选中恒显示）/`filterProjects`/`pickProject`（精确 id 优先、唯一 name 次之、歧义/缺失返回 null 绝不回退）；Start 屏内嵌「打开已有项目」搜索列表 + id 输入，逐行显式选择）。(2) **导入卡真实接线**——`import` 卡 → `openIntakeModal`（client/modals/intake.ts）真实 intake 向导：begin（选目标项目或新建 + source_label + target_phase 驱动 taxonomy）→ stage（multipart ≤32MiB 逐文件上传，`apiMultipart` 原始 boundary 透传 + bearer/CSRF；客户端 32MiB/文件名预检，服务端 413 兜底；已 staged 文件列表带 verdict/删除/续传，sha256 幂等复用提示）→ scan（POST scan，展示 scan_summary + observations + rejected 拒因）→ grill（questions 表单，答案以 `principal:{}` 持久化 human_assertion，revision 携带）→ propose（展示 plan/risks/pre_accept_checklist/置信度/缺口/mappings/required_gates）→ PI adopt（载荷钉定 proposal revision + target project revision + 幂等键，展示 AdoptionReceipt）；每步从 GET intake 投影恢复（重进页面续接），错误显示稳定错误码文案（intake-flow.ts `INTAKE_ERROR_KEYS` 25+ 码 zh/en）。(3) **NextAction 引导**——kernel next-action.ts 新增 intake 覆盖动作（`intake_resume`/`intake_scan`/`intake_answer`/`intake_propose`/`intake_adopt`(capability=pi)，`NextActionContext.intakes` 可选输入、projectProjection 从 listIntakes 投影，终态零动作）；client `intakeGuidance` 将 next_actions_v2 的 intake 动作并入每步引导列表（去重、tone 保留），Overview 卡 route='intake' 点击打开向导。(4) **分块**——服务端为整文件 staged（≤32MiB），UI 如实不做分块，支持「恢复上传」= 已 staged 文件继续/重传（幂等）与删除后重传。证据：tests/unit/intake-flow.test.ts 23/23（三入口选择、状态机每步可恢复、错误码映射、adopt 需 principal、引导映射、zh/en parity 零缺 key）、next-action.test.ts 16/16（intake overlay 全状态 + 终态零动作 + kernel 集成 begin→stage→scan 投影）、ui-simple.test.ts 15/15、next-action-cards.test.ts 29/29、i18n-chrome.test.ts 3/3、根 pnpm test 全绿、research-ui typecheck+build 全绿、run-upload-tests.sh / run-hardening-tests.sh（standalone 段）不破坏、verify-docs 19/19。剩余（浏览器视觉验收，Playwright 类环境不可用，如实保留，记 NOT_RUN_MANUAL_PENDING）：向导拖拽/上传交互、scan/grill/proposal/adopt 同页观感、Start 屏列表观感、真实浏览器多步断点续接——队列见 manual-acceptance.md §4。 | 首次进入显式 Init/Resume 选择；导入卡接真实 intake begin→stage/upload→scan→grill→proposal→PI adopt；支持阶段选择、分块恢复和冲突处理；每一步都投影结构化 NextAction。**代码侧已按左侧证据闭环；浏览器视觉验收 NOT_RUN_MANUAL_PENDING**。 |
| P1 | WORK-01、PTY-01、TRAJ-01、SUBAGENT-01：部分 | **trajectory/topology UI 逻辑层已实现(commit aa6d3e0)**——client 新增 Trajectory 面板(panels/trajectory.ts + trajectory-model.ts:Research 权威/Session 观察双 lane 分组渲染模型、per-lane (event_seq,event_id) keyset 分页状态机(load-more,dedupe 幂等)、服务端 redacted summary 原样展示、allowlisted detail 点击展开)+ Topology 面板(panels/topology.ts + topology-model.ts:直系 tree 懒展开/cycle-safe 扁平化、breadcrumb 逐级返回 parent、child 详情(状态/只读 history)、one-shot 只读 follow-up——POST /v1/topology/{child_id}/followup 返回 message_id 不激活 child);两者挂入 More 导航(#tab=trajectory / #tab=topology 稳定深链),i18n 新增 trajectory/topology namespace zh/en 全 key + shell.tab.trajectory/topology(localeParity 与 chrome 扫描保持绿);证据:tests/unit/trajectory-ui.test.ts 32/32(双 lane 分组渲染模型、分页状态机、tree 扁平化/展开状态、breadcrumb 组装、followup 只读调用模型、缺 key 告警)、根 pnpm test 全绿、research-ui typecheck+build 全绿。Kernel 已有 workspace、真实本机 PTY、trajectory/topology 路由;Terminal 仍是 Job SSE 只读日志,没有 stdin/resize/signal;没有 VS Code 式文件树/多标签编辑。**PTY TUI client 逻辑层已实现(commit 待定主代理统一提交)**——client 新增 pty-session-model.ts(纯逻辑,注入 transport+scheduler 可单测)+ panels/pty.ts(挂入 More 导航,#tab=pty 稳定深链):会话状态机 idle→opening→open⇄detached→closed/error;control 队列(bytes/resize/INT/TERM/KILL/close)client_seq 单调自增、单帧 in-flight、失败重试重发同 seq(服务端幂等不重复)、409 out-of-order 经会话行 resync(已应用→推进,落后→error 提示重连);frames 消费 after_seq 增量轮询(SSE 记后续)、页面 gap/显式 gap 帧处理、retention 截断提示(retained_from_seq/dropped_bytes)、exit 帧、显示缓冲有界;detach 停轮询(进程服务端存活)、reconnect 从 serverSeq 重放、generation 变更提示新会话周期;idle TTL/lease 过期/权限撤销/adapter 失败关闭经周期性会话刷新检测并映射 close-reason 提示;403 lease_invalid/lease_required 致命→error+提示重连/重新 open;错误码→稳定 i18n key 映射;open 表单(workspace 选择/preset/cwd/cols/rows/钉定 profile+target)。i18n 新增 pty namespace zh/en 全 key + shell.tab.pty(localeParity 与 chrome 扫描保持绿)。证据:tests/unit/pty-client.test.ts 36/36(状态机、client_seq 单调/重试幂等/out-of-order resync、frames 增量/gap/retention/exit/重放幂等/显示上限、detach-reconnect 重放、generation、idle TTL/lease 关闭检测、lease 失效、错误码映射、nav 深链、双语求值零缺 key)、根 pnpm test 全绿、**Workspace tree client 逻辑层已实现(commit 待定主代理统一提交)**——client 新增 panels/workspace.ts + workspace-model.ts(纯逻辑层):树模型(flat 列表按父目录分组 + 懒展开、implied dir 客户端投影(listSince 喂不带 dir 节点)、空目录虚拟节点——服务端目录由路径前缀投影无 dir-create、选中状态);操作模型(create 文件 expected_version=0 create-if-absent、CAS 保存 version/etag→409 冲突提示重载(markWorkspaceTabConflict + reloadWorkspaceTab,绝不静默覆盖)、delete/move 源 CAS(move 目标已存在 409→换名提示)、readVersion 历史回退=旧字节以当前 version/etag 守卫写回);二进制(multipart POST /assets ≤32MiB 服务端 sha256、客户端 binaryTooLarge 预检、CAS 守卫可选;下载 GET /blobs?path= 原始字节+节点 media——认证 fetch→object URL);watch/listSince 增量合并(变更 upsert + delete tombstone 含后代 + implied dir 重投影、幂等收敛;面板 5s 轮询,离开 tab 停轮询(stopWorkspaceWatch),SSE 记后续);search=路径过滤(客户端 substring + 服务端 prefix/glob PATH 搜索,内容搜索未实现如实记录);多标签(path/version/etag/content/savedContent,dirty 语义与 manuscript-dirty isEditorDirty 一致—— 真实值,清空读 dirty 恢复读 clean;保存 409→conflicted+重载提示;watch 服务端版本前进→tab conflicted);工具栏(新建/上传/刷新/搜索)+左树右编辑区(textarea+保存;二进制只读 meta+下载)+历史视图(revision 列表+回退,op 文案 zh/en)。挂入 More 导航(#tab=workspace 稳定深链),i18n 新增 workspace namespace zh/en 全 key + shell.tab.workspace(localeParity 与 chrome 扫描保持绿)。证据:tests/unit/workspace-client.test.ts 32/32(树懒加载/展开状态、虚拟目录、CAS 409 冲突重载/etag 传递、move/delete 模型、历史回退、多标签 dirty/冲突/watch 服务端变更检测、二进制上传下载模型、路径搜索过滤、nav 深链、双语求值零缺 key)、根 pnpm test 全绿(54 文件 934/934)、research-ui typecheck+build 全绿。 | 剩余(浏览器层,Playwright 类环境不可用,记 NOT_RUN_MANUAL_PENDING):可操作 PTY TUI 浏览器终端渲染(ANSI/xterm 类)、键盘输入、resize 拖拽与窄屏验收——PTY TUI client 逻辑层已实现(commit 待定主代理统一提交),浏览器终端渲染/键盘/窄屏验收 NOT_RUN_MANUAL_PENDING;workspace tree/tabs/search/watch/upload/move/history/problems(**Workspace tree client 逻辑层已实现(commit 待定主代理统一提交)**;浏览器文件树渲染/拖拽上传/多标签视觉/窄屏/键盘验收 NOT_RUN_MANUAL_PENDING)、trajectory 双 lane 滚动/虚拟化(10k DOM 有界)与键盘/a11y、subagent DAG 浏览器视觉验收(树展开/进入 child/follow-up 交互观感、跨项目 child ID 与撤权观感);本机与远端 PTY 共用鉴权 wire;桌面/窄屏、断线、冲突与 a11y 浏览器验收。 |
| P1 | CONFIG-01、UI-02、UI-03：已实现未验收（2026-08-11 修复轮；浏览器视觉验收 NOT_RUN_MANUAL_PENDING） | **代码侧已闭环（2026-08-11）**：Settings 由 `/v1/config/schema` + `/v1/config/effective` 动态生成（settings-model.ts `settingsConfigModel` 纯模型 + modals/settings.ts 接线；registry 数据不可用时 settingsSectionsForData(false) 回退占位）。每 ConfigScope 一组 Accordion（global/project/job/runner-profile/orchestrator/kernel/standalone 七组，覆盖注册表全部 55 键；job 组保留为空并注明 reserved），每字段展示：effective 当前值（服务端已 redacted；secret 字段只渲染"已设置,不显示明文"掩码，明文永不回显——SecretRef 存储层属后续，如实记录）、scope、声明来源（registry `sources` 的客户端镜像，settings-model.test.ts 对真实注册表逐键钉死防漂移）、安全基线标记、env 别名、schema 描述与默认；config pin 显示（effective.config_pin，本地记忆上一次 pin，变化时提示"config pin 已变化"）；热生效/需重启按声明来源推断（注册表无 hot_reload 标记，如实记录：含 http/ui 来源 → "保存后即时生效"；仅 cli/env/file → "需重启生效"），推断规则在 docs/config-registry.md §6 注明并与测试一致；本地校验（validateSettingsField：number 整数+边界、boolean、enum、string minLength/pattern）与服务端错误回显映射（mapSettingsServerErrors：validation_error/unknown_config_key/security_floor_violation → 字段级错误，未知名键如实 unmatched 不伪造字段）为写面就绪；**写接口不存在（本修订 kernel 仅 GET effective/schema，未知 config 子资源 404）→ 提交按钮禁用并注明"当前配置只读,经 CLI/env 提供"（如实，非假按钮）**。i18n：55 个字段 label + 7 scope 标题 + 来源/scope/热重启/掩码/只读/校验等元信息键全部 zh/en 双字典（shell.ts），localeParity + i18n-chrome 扫描保持绿，动态模型双语求值零缺 key。**Init/Workspace/Trajectory/Topology namespace**：Init 已由 intake 命名空间覆盖（intake-flow.ts INTAKE_ERROR_KEYS zh/en，2026-08-11 既有，非本轮新增）；Workspace/Trajectory/Topology 尚无逻辑层组件（浏览器工作台/拓扑属 NOT_RUN_MANUAL_PENDING），待页面落地时在同一提交补 zh/en namespace + 切换与缺 key 负向测试（如实记录，不伪造 namespace）。证据：tests/unit/settings-model.test.ts 21/21（schema→字段模型全量覆盖、sources/热重启镜像与真实注册表逐键相等、secret 掩码、absent 默认、pin 提取、写模式只读、settingsSectionsForData、本地校验 8 码、服务端错误映射、55 label 双语存在 + 全模型双语求值零缺 key + localeParity）、config-registry.test.ts 30/30、ui-simple.test.ts 15/15、i18n-chrome.test.ts 3/3、i18n-runtime.test.ts 12/12、根 pnpm test 全绿、research-ui typecheck+build 全绿、node scripts/verify-docs.mjs 19/19。剩余（浏览器视觉验收，Playwright 类环境不可用，如实保留，记 NOT_RUN_MANUAL_PENDING，队列见 manual-acceptance.md §4）：动态 Settings 同页观感（scope 组、字段值/掩码、meta chips、pin 变化提示、只读注记）、zh/en 切换即时生效、640/720/1024 视口；`/bff/research/config/*` 写面与 SecretRef 存储层落地后启用编辑（本地校验与错误映射机制已就绪）。 | Settings 由 config schema/effective 动态生成并覆盖所有配置项（已实现——7 scope 组全 55 键，schema/effective 驱动）；SecretRef 不回显（已实现——客户端仅掩码渲染，服务端 redacted 保证明文零回显；SecretRef 存储层属后续，如实记录）；展示来源、scope、校验、config pin、热生效/需重启（已实现——来源为注册表 sources 镜像并由测试钉死、热/重启按来源推断且文档注明、本地校验纯函数+服务端错误映射就绪、pin 显示+变化提示）；写接口不存在 → 提交禁用并注明只读（已实现，如实记录）；所有新增页面在同一提交补 zh/en key、切换与缺 key 负向测试（已实现——55 label+7 scope+元信息键 zh/en 齐备、双语求值零缺 key 负向测试；Workspace/Trajectory/Topology 无逻辑层组件，待页面落地时补 namespace，如实记录）。 |
| P1 | WORK-01、CI-01：已实现未验收 | **代码侧闭环（2026-08-11 修复轮）**：① workspace 权限契约不再依赖 umask——新增 `fs-modes.ts mkdirMode`（mkdir 后只对**新建**目录显式 chmod，既有目录不动）：workspace-store.ts 的 workspacesRoot/原子写目录链/ensure 根/move 目标全部接入 0750，文件写后 chmod 0640 兜底（writeFileSync mode 同样被 umask 剥离，0077 下 0640→0600）；同类 mkdir/mode 一并显式校准：backup.ts backups 目录 0700（文件本已显式 0600）、kernel.ts uploads staged/intake-staged 0700 + 0600 文件 chmod、pty-local.ts runtime 目录 0700（bridge 0600）+ PTY workspace sandbox 0750。② CI 不再排除安全单测——`.github/workflows/ci.yml` 移除 `--exclude tests/unit/security.test.ts`（该 workflow 是辅助 CI，用户主网关为 scripts/ci-gate.sh）。③ pnpm 调用入口固定——packaging.test.ts `resolvePnpm`（优先 `process.env.npm_execpath`（pnpm 运行自身时的 CLI 路径，如 …/pnpm.mjs）→ `pnpm --version` PATH 探测 → 明确安装提示；npm_execpath 场景注入含 `pnpm` shim 的 PATH 前缀，使嵌套 lifecycle `pnpm` 调用可解析）+ run-selfmod-tests.sh `resolve_pnpm` 同设计；根 package.json 声明 `packageManager: pnpm@11.20.0`。证据：tests/unit/workspace-store.test.ts 16/16（新增 umask 0077 用例：workspaces 根/项目根/嵌套目录/.ws-meta history 全链 0750、树文件与历史副本 0640、既有 0700 目录不被改写、原子写无 tmp 残留）、tests/unit/packaging.test.ts 10/10（无 pnpm PATH 模拟：npm_execpath 路径全绿、无 pnpm 时明确报错非 ENOENT）、tests/unit/security.test.ts 26/26、bash tests/security/run-selfmod-tests.sh 19/19、node scripts/verify-docs.mjs 19/19、根 pnpm test 本轮 48 文件 753 测试（本两项相关文件全部通过；并行轮次 PTY 权限收紧（P0-2）的测试/实现同步中状态为当时唯一失败，不属本两项）。剩余：CI job 绑定（辅助 workflow ci.yml 已同步，主网关 scripts/ci-gate.sh）。 | 对完整目录链显式校准并验证权限（已完成，umask 无关）；CI 不排除安全单测（已完成）；package manager 版本与调用入口固定（已完成）；所有阻断 job 零 SKIP、当前 commit 证据可复现（剩余：CI job 绑定）。 |
| P2 | STORE-01、WORK-01：已实现未验收 | **代码侧闭环（2026-08-11）**：① **workspace 恢复协议 = op-ledger 回放 + 启动恢复扫描**（workspace-store.ts `scanWorkspaceIntegrity()`，kernel 构造期自动运行（`recoverWorkspacesOnOpen` 可关）+ 按需调用）：对每个 workspace 比较磁盘字节（每节点 size/sha256）与 `workspace_nodes` 元数据，并按 `workspace_ops` ledger（create/write 携带目标 version+sha256、delete 携带被删 version+sha256、move 携带目标 path+from_path+version+sha256——内容足以回放）修复或隔离。每个 fs/DB 边界崩溃窗口的处理：写路径 rename 后 row 更新前崩溃（磁盘新字节+旧 row）→ 按磁盘字节前滚 row（version+1/hash/size/media，补记 op；二进制节点要求新字节已在 CAS——writeBinary 先注册 CAS 再 rename，不在 CAS 则视为篡改、从 CAS 恢复已提交字节）；row 指向缺失字节 → 二进制从 artifact CAS 恢复精确字节；文本节点本版本字节保留于 history（仅 delete 路径先 keepHistory 再 unlink）→ 前滚完成 in-flight delete（undo 仍可回退）；move rename 后未落库 → 孤儿字节与源 row hash 匹配则回滚 re-associate，目标 row 已插入则前滚完成 move（源 row 删、move op 记 from_path）；孤儿磁盘文件（无 row 无 op）→ 未提交 create 回滚删除；已删 row 但缺 delete op → 补记 delete op（ledger 完整）；`<name>.ws-tmp-<8hex>` 孤儿临时文件（树内 + .ws-meta 历史区）清理，被 row 覆盖的同名文件不误删。**无法可证修复即隔离**：`workspaces.quarantine` 持久化标记（migration 0018，SCHEMA_VERSION 16，幂等 ensureColumn；0011 已发布 WORKSPACE_DDL 不原地改，store 自身连接同步收敛——STORE-08），隔离 workspace 所有读写/move/delete/history/watch/快照一律 503 `workspace_inconsistent`，直到字节恢复后下一次扫描干净收敛自动清除（自愈，无手工 flag）。所有修复幂等（double-scan 状态收敛一致）；扫描修复在单事务内应用（row+op+revision 原子）。② **fleet 内存态结论**：确认 AgentOutboundSpool、InMemoryAgentRegistry、RemoteFleetServer 的 pending/outstanding/stages/claimedJobIds 全部为内存态（无磁盘 spool），**不引入大持久化框架**——重启丢失后按既有 lease 过期语义自愈：agent 重启后重新 register/heartbeat；fleet 重启后 kernel lease 过期（300s TTL）→ 旧 claim 的后续写入 409 lease_stale、job 回 queued retryable → fleet 重新 claim 分发；spool 内存条目随 agent 进程丢失 → terminal 帧缺 seq（kernel retention/gap 语义兜底），业务终态仍由 complete/cancel transaction 决定。结论与自愈语义已记录于 remote-runner-wire.md §5.3 / execution-runtime.md §5.1。证据：tests/unit/crash-recovery.test.ts 11/11（rename-before-row 前滚+幂等、孤儿 create 回滚、delete 前滚+undo 保留、无副本文本节点隔离+重启持久+恢复字节自愈、binary CAS 恢复、move 窗口 1 回滚/窗口 2 前滚、tmp 清理+同名校保护、多窗口双扫收敛、构造期扫描+opt-out、健康 workspace 零改动）、tests/unit/workspace-store.test.ts 16/16、tests/unit/backup.test.ts 2/2、tests/unit/migrations.test.ts 16/16（0018 列/rewind 幂等/标记持久）、根 pnpm test 866/866、research-schemas/research-kernel build 全绿、tests/security/run-workspace-tests.sh 41/41（新增 ws-crash-recovery 3 断言：真实 kernel 进程 kill+重启同 dataDir——不可修复文本节点启动隔离 503 workspace_inconsistent、恢复字节后重启自愈可读、rename-before-row 启动前滚 v2+新 hash；原 38 断言全部保留）、node scripts/verify-docs.mjs 19/19。剩余：CI job 绑定；真实 kill -9/双进程/长期 recovery drill（人工队列，记 NOT_RUN_MANUAL_PENDING，见 manual-acceptance.md §4 与 acceptance-tests.md §21 `workspace-crash-recovery-idempotent`）。 | 引入可证明的恢复协议（已完成——op-ledger 回放 + 启动恢复扫描，见 storage-migrations.md §10.1）；启动时修复/隔离不一致（已完成——修复前滚/回滚/回退 + `workspace_inconsistent` 隔离，重启持久、恢复字节自愈）；对每个 fs/DB 边界注入崩溃并验证幂等恢复（已完成——crash-recovery.test.ts 11/11 覆盖写/删/移/二进制/孤儿/tmp 全窗口 + 双扫收敛 + HTTP 真实重启段）。剩余：CI job 绑定与真实 kill -9/双进程 drill（NOT_RUN_MANUAL_PENDING）。 |

SELFMOD-01 的当前边界保持不变：Cordis self-referential 工具已经以 `configs/research-dev-selfmod.cordis.yml` + `scripts/start-selfmod-dev.sh` 的显式开发 overlay 接入，生产 profile/patch 不包含 tool-cordis；在 tarball 隔离、mount/unmount 清理、审计记录全部通过前不得升级为“已验收”，也不得默认启用。

## 6. 2026-08-11 开发验证方式决策

当前阶段采用“代码优先、人工后验”：真实浏览器、DSH host、远端机器/mTLS、Docker/TeX/GPU、网络分区、长期 recovery 和 clean-room 环境暂不作为代码实现、提交或合并的前置条件。每个 §5 阻断项先完成生产代码、Schema/迁移/i18n、可运行的本地 build/typecheck/unit/contract/static checks，并把真实环境步骤写入 `acceptance-tests.md` 与 `manual-acceptance.md`；完成后标“已实现未验收”，继续下一项。

以下边界不因延后真实环境验收而放宽：不得保留假按钮、宿主正式执行、可选生产鉴权、跨项目放行、伪造 Evidence/Manifest、测试专用生产分支或文档虚假完成声明；本地可复现的失败（例如 §5 workspace 权限失败）仍属于代码缺陷，不能归入“环境待人工”。真实环境人工测试通过并留下结构化记录后，才允许把相应项升级为“已验收”；人工发现的每个新问题仍先内化到 Markdown 再修复。
