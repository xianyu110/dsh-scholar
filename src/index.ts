/**
 * @dsh-scholar/research-plugin — DSH Research OS bundle entry.
 * The package main must export the Cordis plugin (`name`, `inject`, `apply`)
 * for the Loader to activate it as row `@dsh-scholar/research-plugin`.
 * @module @dsh-scholar/research-plugin
 */

export * from './plugin/index.js'
